{
  "name": "gantry",
  "private": true,
  "version": "0.0.1",
  "description": "The pipeline view of multi-agent work — states, gates, handoffs — over any source.",
  "workspaces": [
    "packages/*",
    "adapters/*",
    "apps/*"
  ],
  "scripts": {
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
