{
  "name": "gantry",
  "private": true,
  "version": "0.0.1",
  "description": "The pipeline view of multi-agent work — states, gates, handoffs — over any source.",
  "workspaces": [
    "packages/*",
    "adapters/*",
    "apps/*"
  ],
  "scripts": {
    "typecheck": "tsc -b",
    "snapshot": "tsx adapters/atrium/src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
