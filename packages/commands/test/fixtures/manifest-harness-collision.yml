# fixture: W-069 behaviours 7 and 9 — a harness-less seat whose model a
# codex listing also claims, reproducing the (model, harness) collision the
# live manifest stopped exhibiting once ui-lead gained `harness: codex`
# (see the brief, "The false-green example dissolved, the design did not").
# Also carries the two `tiers[].model` cases behaviour 7 pins: one resolvable
# only through the listing (no entry unless some other source already names
# a harness for it), and one resolvable through nothing at all (no entry,
# no turn).
bisellium: 1
studio: Harness Collision Fixture
patron: patron
collegia: [ { id: engineering, name: Engineering, magister: eng-lead } ]
sellae:
  - { id: eng-lead, collegium: engineering, kind: agent, model: claude-opus-5 }
  - { id: collider, collegium: engineering, kind: agent, model: gpt-6-astra }
  - { id: codex-lead, collegium: engineering, kind: agent, model: gpt-5.6-sol, harness: codex }
probationes: []
tiers:
  - { id: escalation, model: gpt-6-astra }
  - { id: fast, model: gpt-5.6-luna }
  - { id: orphan, model: solo-orphan-model }
munera: []
