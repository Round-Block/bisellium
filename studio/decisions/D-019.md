---
id: "D-019"
title: "W-028 (patron-as-agent orchestration) is abandoned"
at: 2026-09-20T00:00:00Z
provenance: stated
by: patron
kill_when: "a successor patron-as-agent opus is filed — its brief must start from current architecture, not W-028's; resurrection of W-028 itself reopens its evidence obligations in full"
---

The Patron's disposition of 2026-09-20: close W-028 ("patron-as-agent:
automated merge, push and cascade orchestration") — the brief is outgrown,
the Patron's toggle/checklist automation vision has moved past it. Re-file a
fresh opus when that capability is actually wanted.

`bisellium close` refused it (`W-028: gates not passed: review`, reproduced
in `ci/cascade-23-cleanup-review.log` §6) — `close` is a finish verb, and it
gates on passed reviews. W-028's review failed at round 3
(`ci/W-028-review-3.log`) with no path forward this cascade, so the
abandonment executes as `bisellium halt` (W-042's verb) instead, with this
decision as its authority anchor.

W-028's five missing reds (behaviours 1 through 5, `opus.red_evidence`) and
its stale `tests`/`lint`/`types` certificates (`tree:5ab03a0b…` against the
current tree) are accepted as historical, never to be backfilled — that
would be exactly the backfill `docs/ADOPTION.md` and the engineering lex
forbid.

kill_when fires the moment a successor patron-as-agent opus is filed: that
successor's brief starts from current architecture, not W-028's frozen one.
Resurrection of W-028 itself is a separate trigger — it means the work
resumes, and resumed work owes its evidence in full again; this decision
only accepts the gap for the halted period, it does not retroactively excuse
a resurrected opus's obligations.
