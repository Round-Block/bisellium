---
id: "D-017"
title: "W-023 and W-027 are halted by acceptance, missing evidence accepted as historical"
at: 2026-09-20T00:00:00Z
provenance: stated
by: patron
kill_when: "either opus is resurrected — resurrection reopens the evidence obligation in full"
---

Decreed 2026-09-20, as part of cascade 23's cleanup (W-038's class: a known
CLI gap — there is no per-opus halt verb, only officina-wide `pause`).
W-023 and W-027 both sit `building` with a failed review and no path
forward this cascade; halting them by acceptance rather than leaving them
open against evidence that will never be completed by hand.

- **W-023** (containment scoping and helper hoist) — halted. Its review
  gate failed at round 2 (`ci/W-023-review-2.log`) and its `tests`/`lint`/
  `types` certificates are stale (`tree:337fafe6…` against the current
  tree). Accepted as historical, not backfilled.
- **W-027** (container lifecycle: prune, cleanup and disk-aware cascade
  start) — halted. Missing reds for behaviours 1, 2, 4 and 5
  (`opus.red_evidence`) and stale `tests`/`lint`/`types` certificates
  (`tree:5ab03a0b…` against the current tree) are accepted as historical,
  not backfilled.

Neither opus's outstanding evidence is produced after the fact — that
would be exactly the backfill `docs/ADOPTION.md` and the engineering lex
forbid. The gap this decision papers over is real: no CLI verb halts a
single opus (`pause` is officina-wide only), so both opera stay recorded
`state: building` with a handoff naming the blocker, and `opus.red_evidence`
keeps reporting the missing W-027 reds honestly until a halt verb exists
or one of these opera is resurrected.

kill_when fires the moment either opus is picked back up: resurrection
means the work resumes, and resumed work owes its evidence in full again —
this decision only accepts the gap for the halted period, it does not
retroactively excuse a resurrected opus's obligations.
