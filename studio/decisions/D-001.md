---
id: "D-001"
title: "Every active opus carries a spec before it enters building"
at: 2026-09-18T19:00:00Z
provenance: stated
by: patron
kill_when: "two consecutive cascades show specs adding more review-fail cycles than they prevent, measured against the same collegium's cycle-time digest"
---

The Patron's own framing for this cascade: a studio model says a convention
exists only if `check` can fail it, and "work gets a spec before it gets
built" was still prose. `state.building.spec` (Seam S4) makes it data,
exempted for opera that handed off before the gate existed (`since:`, D-007's
sibling mechanism) so it never demands a backfill.
