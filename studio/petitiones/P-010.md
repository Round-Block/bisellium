---
id: "P-010"
from: producer
to: patron
state: resolved
opened: 2026-09-20T08:30:00.000Z
---

Proposed amendment to the production lex (`studio/leges/production.md`).
No `opus:` — this proposes law, not work. (The gap in writing this by
hand is already recorded at W-038; not re-litigated here.)

**§2 (Decides alone), replace the sequencing bullet:**

> - Sequence within the slate, ranked by long-term positive gain —
>   compounding value to the process and the product outranks one-off
>   convenience (Patron's decree, 2026-09-20); halt an item on collision;
>   reassign sellae

**§2 (Decides alone), add — deliberately without a `(check:)` marker:**

> - A gap discovered in the tooling or process becomes a lesson or a
>   backlog opus within the cascade that found it — never handoff prose
>   alone

*(Grepped `packages/cli/src/rules/` and `check.ts` first: no rule is keyed
on a single gap going unconverted. The nearest, `lesson.recurrent`, fires
only once a `class` recurs across >=2 cascades — silent on the first,
unconverted find this clause targets. Marking it with a rule id that
doesn't enforce it would be exactly what `lex.marker_unknown` catches once
W-035 ships that rule. So it goes in unmarked; `lex.unchecked` will count
it, correctly, until an enforcing rule lands or the clause is withdrawn.
That trade — a lex clause with no automated teeth yet — is yours to accept
or reject.)*

This clause's enforcement path is not a new rule today: it is W-035's
`addressed_by` ledger (P-009) reading this clause's obligation once that
ledger exists — accepting P-009 is most of what closes this one.

**§9, one row:**

> | 2026-09-20 | Backlog ranked by long-term positive gain; a found gap owes a lesson or opus, not handoff prose (unmarked) | Patron |


[stated] 2026-09-20T09:16:55.357Z patron: Accepted as written, 2026-09-20 — both clauses: the backlog ranked by long-term positive gain, and a found gap owing a lesson or backlog opus within its cascade, never handoff prose alone. Clause 2 unmarked until an enforcing rule exists, per the petitio's stated trade.