---
id: "P-004"
from: eng-lead
to: patron
state: needs_you
opened: 2026-09-19T13:00:00.000Z
opus: W-021
---

W-021's insurance clause (P-001 items 2-4, adopted verbatim): build
`path.id.unvalidated` and `path.escapes.officina`, run them over both
officinae, and if either fires anywhere it clearly should not, land it
`advise` rather than bend the tree to a new rule. It fired.

**Firing site:** `studio/acta/2026-09-17-kickoff.md` — its `evidence:` entry
`{ label: dossier build order, href: ../README.md }` cites the repo-root
`README.md` from inside the officina. `path.escapes.officina` reads that as
an escape (it resolves to the repo root, outside `studio/`), but the
existing `checkLink`/`acta.evidence` rule has always accepted it — an acta
digest evidence href is officina-relative, not required to stay inside the
officina, and repo-root docs (`README.md`, `CLAUDE.md`, `docs/*`) are
exactly the kind of thing a kickoff digest cites. There is nothing wrong
with that entry.

No other firing site was found: `examples/sample-studio` produces zero
findings from `path.id.unvalidated`/`path.escapes.officina`, and this is the
only occurrence anywhere in `studio`.

**Action taken (per the clause, without editing another opus's source):**
both `path.id.unvalidated` and `path.escapes.officina` land at `advise`, not
`block`, in `packages/cli/src/rules/paths.ts`. D-008's promotion to blocking
therefore did not ship blocking this cascade; the detector exists, is
dogfooded (no `startsWith` containment test in its own module), and is
wired into `checkStudio`, but at advisory level pending a decision here.

**Asking the Patron:** either (a) narrow `path.escapes.officina` to exclude
acta evidence entirely (evidence hrefs were never contractually
officina-contained), or (b) accept that acta evidence may legitimately point
at repo-root docs and scope the rule to `opus.spec` / gate `evidence:` only
(both of which are contractually officina-relative today), or (c) something
else — and confirm whether D-008 should then re-promote to `block` once the
scope is settled.
