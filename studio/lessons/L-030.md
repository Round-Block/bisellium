---
id: "L-030"
at: 2026-09-24T23:19:09.379Z
class: "mechanism×vendor-spend-unmetered"
evidence: ["ci/retro-32-evidence.log","aerarium/2026-W38.yml","acta/2026-09-24-retro-28.md"]
cascade: 32
---
Filed by `bisellium retro --cascade 32`. 1 finding(s) in this cascade.

**Appended 2026-09-25T01:46:33Z by qa-lead (censor) — W-072 is NOT this class's target.**
W-072 ("the vendor-spend sentinel: npm test fails if any test invokes a real vendor
binary") is recorded as "L-030's target". It addresses a real and adjacent hazard —
unintended in-suite spend, concretely `apps/server/src/http.ts:830`'s
`createListingCache(opts.listModels ?? codexListModels)`, which shells out to the
real `codex` binary whenever a test forgets to inject `listModels` (verified in the
W-065 censor rounds) — but that is TEST HYGIENE, not this class.
L-030 is a VISIBILITY gap and it remains **OPEN and unaddressed**: 425,000 tokens,
18.1% of cascade 32's spend, ran on lanes this officina cannot meter, and there is
no `aerarium/2026-W39.yml` for the period the cascade actually ran in — which is
why retrospectio 32's Usage section reports "(no aerarium on record for the current
period)" and no collegium's posture, including qa's own, can be computed. That
weekly record is the Patron's to write (`aerarium/`'s own header: "Allowances only,
Patron-written"), not the Censor's.
This note exists so lex §3 does not silence a live class: an `addressed_by` naming
W-072 would mark L-030 answered while the metering gap and the missing W39 aerarium
both stand. W-072's recorded title stays as filed (there is no retitle verb —
W-062); its future brief decides whether to widen it or leave L-030 a separate
target.
