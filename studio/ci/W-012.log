BUILD/TESTS. `npm run -s typecheck` exits 0. `npm test -s` exits 0 with zero lines matching ^FAIL. `npx tsx packages/cli/src/writes.test.ts .` reports 54 PASS / 0 FAIL, exit 0. Builder's report reproduced exactly.

HERMETICITY. packages/cli/src/writes.test.ts pins NOW = new Date("2026-09-18T14:00:00Z") and passes it as opts.now to every call; every scenario runs on an mkdtempSync + cpSync copy of examples/sample-studio and is rmSync'd in a finally block. No spawn/exec, no reference to claude or codex in the test file.
