# W-046 — provider portability: the decreed model and a command policy reach both harnesses

**Proposed record title change.** The record reads "provider portability:
one opus end-to-end on both harnesses, model and policy passed". This brief
delivers the second clause and explicitly does not deliver the first: what
crosses both harnesses here is a **talk-shaped dispatch** (a bounded,
read-only, context-carrying turn), not an opus. Sol's round-1 finding 1 is
upheld. The title above is the honest one; the record is the orchestrator's
to change through the CLI. Opus-level portability is filed as the follow-on
named in Out of scope, and nothing in this brief may be read as delivering
it.

Spec of record, signed by the architect, 2026-09-24. **This document is
signed by the architect alone.** A spec that reads as censor-signed
compromises the independence D-014 exists to protect (P-008): the censor's
findings are inputs to it, exactly as Sol's are, and every amendment below
is the architect's judgment on them. The commit carrying this revision names
the architect and no one else in its trailer (round 2's amendment commit
wrongly carried a censor co-author; the orchestrator's error, recorded here
as O-11 so the record shows it was caught rather than repeated).

Revised three times the same day: first after Sol's red-team (UNSOUND, 6
blocking — all six accepted, three of them by removing or narrowing a claim
rather than by adding work); then after the censor's round-1 review
(`studio/ci/W-046-review-1.log`: FAIL, F-1 and F-2 blocking, F-3 advisory);
then after round 2 (`studio/ci/W-046-review-2.log`: FAIL, F-1 and F-3
**closed**, F-2 **partially** closed, one new blocking **F-4** whose root is
behaviour 7's scenario shapes — a stub modelled a vendor failure the vendor
does not produce, so the fix was built to an incomplete diagnosis,
faithfully). The architect re-measured F-4's live envelope before amending:
`claude -p --model gpt-6-astra` exits 1 with a **valid 1284-byte JSON
envelope on stdout** (`is_error: true`, `api_error_status: 404`, no `error`
key, the cause in `result`) and 78 bytes on stderr — so `raw: parsed` holds
neither a `stdout` nor a `stderr` key and the shared reader finds nothing.
Round 3 amends behaviour 7 only. The censor checked all eleven
claims in "Measured on this machine" against the installed vendors; eight
held and **three did not**, so the amendment is the architect's before the
builder's fix round. Each refuted claim is corrected where it stands and
labelled `CORRECTED, round 1`, because the follow-on opus inherits this
section. Two behaviours are added (7 and 8), dispatched as one change. The
architect independently reproduced F-1's egress probe and F-3's envelope
inspection before amending; F-2's mechanism is visible in the source and
was not re-run. Inputs
reconciled: `docs/research/agent-studio-landscape-2026-09-21.md` (the
standing landscape: `talk` has Claude and Codex profiles but passes neither
the requested model nor an equivalent command policy; hooks are Claude-only;
dispatchers may own leases/retries/quotas/recovery while files, decisions,
checks and certificates stay authority), `docs/research/herdr-landscape.md`
(the dispatch envelope: Bisellium constructs sella, decreed model, policy,
bounded task, working directory and context; the adapter returns transport
facts only), and `studio/decisions/D-020.md` (direct `codex exec` is an
interim transport, model tiers are decreed, codex output is input and never
a gate). This opus is cross-cutting and touches a permission boundary, so
Sol red-teams this brief (D-020) as input, not authority. The `ready` gate
is signed on `opus/W-046` after `bisellium branch` (D-021).

## Intent

`talk` is the one seam through which bisellium starts a vendor session
(`performTalk`, `packages/commands/src/talk.ts`; `tick`'s unattended
dailies and `serve`'s `POST /api/talk` both route through it). It resolves a
sella's **harness** from `sellae[].harness` and stops there. The manifest's
`sellae[].model` — the decreed tier, the thing D-020 exists to fix — is read
by **no dispatch path**. (It is not dead data: `adapters/native/src/index.ts`
copies it into snapshot metadata. It simply never reaches a vendor.)

So today a sella declared `model: claude-opus-5` is talked to on whatever
model `claude` happens to default to, and a sella declared
`model: gpt-5.6-sol` is talked to on **claude** (the default harness) with
that same vendor default. The decree is written down and then dropped on the
floor.

The policy half is worse than "missing". W-044 made the claude profile a
deny-by-default permission boundary: `--restricted`, `--strict-mcp-config`,
`--permission-mode dontAsk`, an enumerated `--tools`, and an enumerated
`--allowedTools` admitting exactly `bisellium context|query|check`. The
codex profile spawns `codex exec --json --skip-git-repo-check -` and
nothing else — no policy at all.

### Measured on this machine, 2026-09-24

Not assumed. `codex-cli 0.153.4`, `claude` 2.x, WSL, inside the studio's
bubblewrap sandbox. Sol's round-1 probes agree on the CLI facts; every claim
below that this brief acts on was re-run here.

- **The codex profile's own argv loads the operator's personal config.**
  Running exactly today's argv under the W-049 env projection produced six
  `https://chatgpt.com/backend-api/ps/mcp` connections: the operator's
  `~/.codex/config.toml` declares an `mcp_servers` entry and an enabled
  plugin, and the talked session started them. That file also pins a default
  `model` and a `model_provider`. `codex doctor` confirms: `config.toml
  ~/.codex/config.toml`, `MCP servers 1`, `feature flags 47 enabled`.
- **`--ignore-user-config --ignore-rules` removes that surface.** The same
  run with those two flags produced **zero** MCP connections. Scope of the
  claim, exactly: it removes what the **user config file** declared. It is
  not a general configuration or tool boundary (see the signed limit below).
- **`sandbox_mode=read-only` is a real write boundary, and it holds inside
  the studio sandbox.** A turn asked to run two commands: `cat package.json
  | head -2` returned exit 0 (the read ran), `touch
  <repo>/zz-w046-probe.txt` returned exit 1 and no file was created. No
  nested-sandbox failure: D-020's "codex's own sandbox off inside the studio
  sandbox" lesson does **not** apply to a read-only talk turn, and no bypass
  flag is needed. It bounds writes only — not reads, not MCP, not any tool
  feature.
- **Feature defaults are an open egress surface, and `--disable` closes the
  named ones.** `codex features list` shows `browser_use`,
  `browser_use_external`, `browser_use_full_cdp_access`, `in_app_browser`,
  `apps`, `plugins`, `remote_plugin` and `plugin_sharing` as **stable and
  enabled by default** — none of them governed by `sandbox_mode` or by
  `--ignore-user-config`. W-044 deliberately dropped `WebFetch` from the
  claude profile because "paired with `Read` it would be the profile's only
  egress"; a codex talk session has that egress today. `codex features list
  --disable browser_use --disable apps --disable plugins --disable
  in_app_browser` reports all four as `false`, so the mechanism is real and
  verifiable without a model call. **It closes those eight named features
  and nothing else.**
- **CORRECTED, round 1 (censor F-1, `studio/ci/W-046-review-1.log`): a live
  network egress channel survives the whole policy.** Round 1 of this brief
  asserted "web search proper is already off on this version" from
  `codex features list` (`search_tool` removed, `standalone_web_search`
  false, `web_search_request` deprecated/false). **That inference was
  wrong**, and the feature table is not the tool surface. Measured by the
  censor and reproduced independently by the architect on 2026-09-24, under
  the exact shipped argv including all eight `--disable` names: a turn asked
  to fetch `https://api.github.com/repos/openai/codex` emitted
  `item.completed` / `web_search` with no error and answered `126268`,
  against an independent fetch of `126275` moments later — a live counter,
  seven apart, which is a real fetch and not recall. A second probe returned
  `example.com`'s actual `<title>`. The fetch runs **vendor-side**, so
  neither the studio's bubblewrap sandbox nor `sandbox_mode=read-only`
  touches it, and **none of the eight `--disable` names closes it**. No
  egress-closing flag is specified here, because none was probed: whether
  one exists is the follow-on's question. The fix in this opus is
  documentary — the residual is named in the signed limit, in
  `docs/ADOPTION.md`, in the `codex.ts` header, and asserted by behaviour 6.
- **The whole proposed codex argv works under W-049's ten-name env
  allowlist, unchanged.** `env -i` with exactly `PATH HOME SHELL TMPDIR
  HTTPS_PROXY https_proxy HTTP_PROXY http_proxy NO_PROXY no_proxy` plus the
  policy argv and `-m gpt-5.6-luna` completed a real turn: exit 0, one
  `agent_message`, usage reported. **No env name needs to be added.** The
  qualification is load-bearing and stays in the docs: this works for the
  **default login location only** — `$HOME/.codex/auth.json` (`codex
  doctor`: `auth storage mode File`, `stored auth mode chatgpt`).
  `CODEX_HOME` and `OPENAI_API_KEY` are off W-049's allowlist by design, so
  a custom config home and API-key-only auth do **not** work through a
  bisellium-spawned codex, and that is intended, not a gap.
  Dropping the six proxy names breaks DNS under the sandbox, which is why
  they are on W-049's list already.
- **`codex exec` and `codex exec resume` do not take the same flags.**
  `resume` accepts `-m`, `-c`, `--json`, `--skip-git-repo-check`,
  `--ignore-user-config`, `--ignore-rules`, `--disable` — and **rejects
  `-s/--sandbox`** (`error: unexpected argument '-s' found`). It also has no
  `-C/--cd`. One policy constant can therefore serve both spawns only if the
  sandbox mode travels as `-c sandbox_mode=read-only`. Measured on both:
  start and resume each accepted the identical constant (including every
  `--disable` token), and each denied the write. **This inventory is not
  exhaustive** — `--oss`, `--local-provider`, `--profile`,
  `--approve-for-me`, `--add-dir` and `--color` are also start-only (Sol,
  note 9). No profile may assume a flag crosses; the builder diffs the two
  help outputs (Order of work).
- **A project-level codex config did not override anything.** A fresh
  directory holding `.codex/config.toml` with `model_provider =
  "zzbogusproj"` ran a normal turn on the user config's provider, so that
  key at least is not a project-level channel on 0.153.4. This is one
  measurement of one key in an untrusted directory: it is **not** a proof
  that no project, managed, system (`/etc/codex/config.toml`) or cloud
  default channel exists. Those stay in the residual list, unmeasured.
- **CORRECTED, round 1 (censor F-3): neither vendor reports a model.** Codex
  emits no `model` key in any event. Round 1 of this brief said "Claude's
  JSON envelope does carry one" — **false on claude 2.1.280**, measured by
  the censor and reproduced by the architect: the envelope has no `model`
  key at all (25 top-level keys, none of them `model`), and `modelUsage`
  holds a single key which is the **alias** (`claude-opus-5`), never a dated
  build id. `claude-code.ts` reads `parsed.model`, so `Turn.model` is
  `undefined` for every real turn on **both** harnesses. The design does not
  change: `turn.model ?? requestedModel` in `performTalk` is exactly right,
  and the decreed value is the only truthful thing obtainable — the timeline
  records what was **requested**, always, and behaviour 4 plus mutation M23
  already prove the fallback. What changes is the smoke's pass condition
  (Acceptance), which asked for a build id no implementation can produce.
  Not taken, and recorded so a later reader knows it was considered: reading
  `Object.keys(parsed.modelUsage)[0]` would return the same alias the
  request already carries. It only differs if the vendor silently
  substitutes a model (an overload downgrade, or `--fallback-model`, which
  this profile does not pass). That is a real correctness argument worth a
  line in the follow-on, not a change bought at this round's price.
- **The manifest's model names are valid on the claude harness, and a
  cross-vendor name fails closed and loudly.** `claude --model
  claude-opus-5` and `--model claude-sonnet-5` both returned exit 0;
  `claude --model gpt-6-astra` returned exit 1 with
  `[claude-code:unrecognized_model]`. Consequence worth stating: after this
  opus, a sella whose declared model does not belong to its harness stops
  silently running on the wrong model and starts failing. Two such rows
  exist today — `studio/bisellium.yml`'s `builder-sol` (`gpt-5.6-sol`, no
  `harness` key, so claude-code) and `examples/sample-studio`'s `art-lead`
  (`gpt-6-astra`, same). The real studio's row is fixed here (Files owned);
  the fixture's is left and filed (Out of scope).
- **`claude -p --resume` with `--model` is unproven live.** One attempt to
  resume a session id returned by a previous `claude -p` run answered "No
  conversation found with session ID". That may be a property of `-p`
  session storage rather than of `--model`, and it is **pre-existing**: it
  is the resume path talk already depends on at HEAD. Acceptance now forces
  a live claude resume turn and says what to do if it fails (it is a finding
  against the existing path, reproduced on `master` before it is blamed on
  this opus).
- **A turn can exit 0 with no reply.** D-020's recorded lesson ("output
  non-empty check before trusting exit 0") has a code home:
  `codex.ts`'s `exitCode: code ?? …` trusts the process code verbatim, and
  `claude-code.ts` returns `parsed.result ?? ""` with `exitCode: r.status`.
  Either path can hand `performTalk` a Turn with `exitCode: 0` and
  `reply: ""`, and `performTalk` then prints an empty line, writes a session
  file, appends two timeline entries **and closes a receipt** — a recorded
  success that never happened.

### The signed limit: W-044 equivalence is not achieved on codex

This is the finding Sol forced, and it is stated here rather than in a
review log, because a later brief must not be able to write "codex is
boundaried now".

**What the codex policy achieves:** no writes outside the sandbox mode; no
user-config-declared MCP servers or plugins; no user or project execpolicy
rules; no ambient default model or provider from the user config file; and
the eight named feature flags off — `browser_use`, `browser_use_external`,
`browser_use_full_cdp_access`, `in_app_browser`, `apps`, `plugins`,
`remote_plugin`, `plugin_sharing`. **That list is the whole achievement on
the tool surface: those eight names, not "no egress".** Round 1 of this
brief wrote the line as "no browser, in-app-browser, apps or plugin
feature", which reads as a boundary it does not have; the censor measured
the difference (F-1).

**What it does not achieve, and what W-044 does achieve on claude:**

- **Network egress is open, and it is paired with unbounded reads — the
  exact pairing W-044 refused.** A talked codex session has a working
  vendor-side web fetch/search channel: measured live under the shipped
  argv, twice, with a hallucination control (Intent, "CORRECTED, round 1").
  It survives all eight `--disable` names, `sandbox_mode=read-only` and the
  studio's own bubblewrap sandbox, because the fetch happens on the vendor's
  side of the wire. W-044 dropped `WebFetch` from the claude profile for
  precisely this reason — in its own words, "paired with `Read` it would be
  the profile's only egress". The codex profile ships with **both** halves
  of that pairing: every read the operator can perform, and a way out. This
  is the largest residual, it is named first for that reason, and no flag
  closing it has been probed. Whether one exists belongs to the follow-on.
- **No tool allowlist.** Claude's profile enumerates what exists
  (`--tools`) and what is approved (`--allowedTools`, ten literal rules), so
  a verb the CLI has not shipped yet is denied by construction (W-044
  behaviour 3). Codex offers only a **denylist of named features**, and the
  censor's live tool census found eighteen tools in a policy-applied session
  — `apply_patch`, `exec_command`, `write_stdin`, an image generator, a
  `collaboration.*` agent-spawning family and `web.run` among them. A
  denylist goes stale by the next release — this repo's own W-049 ruling
  says so, and F-1 is that failure already happening: the eight names were
  chosen from the feature table, and the feature table is not the tool
  surface.
- **Reads are unbounded.** `sandbox_mode=read-only` permits every read the
  operator can perform, including `~/.codex/auth.json`. A talked claude
  session cannot run `cat` at all.
- **Instruction files still load.** `AGENTS.md` (user and project) is not
  covered by any flag specified here.
- **Configuration channels beyond the user file are unmeasured.** Project,
  managed, system and cloud defaults: one negative measurement above, no
  proof. Treat as residual.
- **The execpolicy `.rules` mechanism is not the enumeration fix.** Round 1
  of this brief said it was; Sol is right that execpolicy governs which
  commands may escape the sandbox or need approval, not which reads are
  permitted inside it. The upgrade path for a real command allowlist is
  **unknown** and is the follow-on opus's first question, not a claim made
  here.

Every one of these residuals is repeated in the `docs/ADOPTION.md` paragraph
and in the `codex.ts` header this opus owns, and behaviour 6 asserts they
are there. **A residual is added to that list by measurement, never
removed by inference** — F-1 is what removal-by-inference cost: a feature
table said a tool was off, and the tool worked.

### What this opus is, and is not

It makes the Herdr envelope real at the one seam that exists: **the decreed
model travels from the manifest through `performTalk` into both vendor
profiles, and the codex profile gains a command policy of its own, with its
limits signed.** It is not a universal adapter layer, it does not move build
dispatch into the CLI, and it does not deliver one opus end-to-end on both
providers.

D-020's `kill_when` names this opus. Say the residue plainly: bisellium
still cannot dispatch a **build** on either harness — builders are spawned
by the vendor's own Agent tool, and codex builds are still
orchestrator-mediated. W-046 landing retires the interim transport clause
for **talk-shaped** dispatch only. D-020's `kill_when` fires as written — it
triggers a *review* of that decision, not an automatic repeal of its build
clauses.

### What "end-to-end" means as a test

**A fixture officina, in-process, against stub vendor binaries** — that is
the honest testable unit for a talk-shaped dispatch, and it is behaviour 5.
Driving a real `claude` and a real `codex` from a test would put a paid,
non-deterministic, network-dependent call in `npm test`, and the recorded
reds would carry real session ids and real usage. The precedent is W-044 and
W-049: stub binaries that log their own argv, their stdin and their cwd and
never execute what they are handed; evidence is string-level.

The live dual-harness proof is **Acceptance-level, attended, run by the
censor**: the same command against the real `claude` and the real `codex`,
on a temp copy of `studio/`, using two sellae whose decreed models belong to
their harnesses. That proves the *seam*, not an opus. Opus-level
cross-provider evidence is the follow-on's, and D-020's codex-build trial
(W-050) is separate evidence on a separate transport; neither is claimed
here.

## Files owned

- `packages/shim/src/harness/types.ts`: add `model?: string` to
  `HarnessStartOpts` and `HarnessResumeOpts`, with the doc comment below
  (Interfaces). No other change.
- `packages/shim/src/harness/claude-code.ts`: pass `--model` when the
  envelope carries one, in the slot the ordering rule fixes (Interfaces).
  Header comment updated. **Round 3 (F-4):** the parsed-envelope return
  stops discarding captured stderr — `raw: { ...parsed, stderr }` under the
  two guards in behaviour 7. That one expression is the whole change to this
  file this round; `reply`, `exitCode`, `model` and `usage` are untouched,
  and the header comment gains a line saying why `raw` may carry a key the
  vendor did not emit.
- `packages/shim/src/harness/codex.ts`: a module-local
  `CODEX_POLICY_FLAGS`, applied identically by `start` and `resume`; `-m`
  when a model is decreed. Header comment updated, including the signed
  limit — **round 2: the header's achievement line and residual list must
  match the corrected signed limit, egress named** (F-1). **The version
  probe's stdin handling is not touched** (Out of scope, Sol finding 4).
- `packages/commands/src/talk.ts`: read `sellaRow.model`, pass it into
  `start`/`resume`, record `turn.model ?? requested` on the timeline's `out`
  entry, refuse a no-reply turn (behaviour 3), and **round 2: carry the
  vendor's captured diagnostic into the failure message** (behaviour 7,
  F-2). One guard at the shared site, both profiles — never per call site.
  **Round 3 (F-4):** `vendorDiagnostic` is widened to the four shapes the
  profiles actually produce, exactly as behaviour 7 specifies. It stays one
  function at one site; nothing moves into a profile.
- `packages/cli/src/provider-portability.test.ts`: new. Behaviours 1–7.
- `packages/cli/src/talk-boundary.test.ts`: **recorded handoff** — W-044 and
  W-049 are both `done`, and this opus is the recorded reason. Its
  `expectedStartArgv`/`expectedResumeArgv` deep-equal the whole argv, so the
  model slot must be added there or behaviour 1 of that file fails for the
  right change. Nothing else in it changes, and its existing assertions keep
  their meaning. `packages/shim/test/harness.test.ts` and
  `packages/cli/src/harness-env.test.ts` assert argv *forms*, not whole
  arrays; they are expected to stay green untouched. If either needs an
  edit, stop and report — it means the argv moved further than this brief
  says.
- `package.json`: append the new test file to `test`. That is the only
  change.
- `studio/bisellium.yml`: one line — `harness: codex` on `builder-sol`. Its
  decreed model is a codex tier (D-020) and its default harness cannot run
  it; this is the row that makes the dual-harness Acceptance smoke real.
  **Expected side effect, named so the censor expects it** (Sol, note 8):
  hook liveness is reported only for claude-code sellae
  (`packages/shim/src/hooks/receiptStatus.ts`), so `bisellium hooks check`
  will report `builder-sol` alive and `bisellium check studio` will stop
  emitting `hook.dead` for it. A finding disappearing is the intended
  consequence of declaring the harness truthfully; no other sella's
  reporting changes, and context, run, boot files and `BISELLIUM_SELLA` are
  unaffected.
- `docs/ADOPTION.md`, "Running talk": the model-and-policy paragraph.
  Behaviour 6 is its acceptance check. **Round 2, two corrections the censor
  measured against the shipped text** (F-1, F-2): the achievement line must
  not read as "no egress" and the residual list must name the live web
  fetch/search channel; and the sentence claiming a mismatched model "exits
  non-zero (`[claude-code:unrecognized_model]`) and talk relays that failure
  closed" must say what is true after behaviour 7 — talk relays the exit
  code **and** the vendor's own captured diagnostic — and nothing stronger.
  **Round 3 (F-4):** that sentence is currently **overclaiming for the
  `is_error` case** — the censor measured the shipped text promising
  `[claude-code:unrecognized_model]` while the live message carried neither
  that string nor the envelope's `result` sentence. After the widened read
  it becomes true, and it must be pinned rather than trusted: **behaviour 7**
  asserts both that the live-shaped scenario A carries both strings and that
  the shipped sentence says exactly that, so the sentence cannot drift back
  into a promise nothing checks. The doc assertion goes in behaviour 7 and
  **not** behaviour 8 on purpose: 8's log is valid evidence for claims that
  did not move, and only 07 is authorized for re-recording. This is F-1's
  defect class recurring inside
  F-1's own remedy, and the rule that follows from it is already written in
  the signed limit: **a claim enters a shipped document only with an
  assertion that fails when it stops being true.**

Not touched: `run.ts`, `fake.ts`, `git-only.ts`, `env.ts`, `redact.ts`,
`check.ts`, `examples/sample-studio/bisellium.yml`.

## Interfaces

- **Envelope, new fields.** `HarnessStartOpts.model?: string` and
  `HarnessResumeOpts.model?: string`. Doc comment, in both places:

  > The model bisellium decreed for this sella (`sellae[].model` in the
  > manifest). Absent means **no bisellium override** — the caller declared
  > no model, so the vendor decides (its own default, or, on a resume, the
  > model the saved session already runs). Never "look one up". A profile
  > passes this value to its vendor CLI verbatim and reads a model from
  > nowhere else: not from the environment (W-049), not from a vendor config
  > file (this opus's codex policy), not from the session being resumed.

  Documented reason for the seam change: this opus. It is additive and
  optional, so `fake` and `git-only` need no edit.
- **`Turn.model` is unchanged**: what the vendor echoed, or `undefined`.
  The requested-model fallback lives in `performTalk`, one expression,
  because it must hold for every profile and every caller — not per profile.
- **No new CLI flag.** `talk` gets no `--model`. Models are decreed
  (D-020); a per-call override would be a route around a decree, and
  `--harness` already exists for the one axis an operator legitimately
  switches. If an override is ever wanted it is a decision first.
- **Policy is a profile constant, not an envelope field.** The Herdr
  envelope names "permission policy" as a field; bisellium has exactly one
  policy per profile, and W-044's whole point is that a caller cannot weaken
  it. So the model travels in the envelope (it varies per sella) and the
  policy stays a module-local constant in each profile (it does not). The
  day a second policy exists — a build session, which needs write
  permission — the envelope carries a policy **name** and each profile maps
  it. That is the trigger; it is not today.
- **The codex policy, exactly.** One constant, applied identically by
  `start` and `resume`, because `codex exec resume` rejects `-s`:

      ["--json", "--skip-git-repo-check",
       "--ignore-user-config", "--ignore-rules",
       "-c", "sandbox_mode=read-only",
       "-c", "model_provider=openai",
       "--disable", "browser_use",
       "--disable", "browser_use_external",
       "--disable", "browser_use_full_cdp_access",
       "--disable", "in_app_browser",
       "--disable", "apps",
       "--disable", "plugins",
       "--disable", "remote_plugin",
       "--disable", "plugin_sharing"]

  `start` emits `["exec", ...POLICY, ...model, "-"]`; `resume` emits
  `["exec", "resume", <id>, ...POLICY, ...model, "-"]`, where `...model` is
  `["-m", <model>]` or nothing. The prompt is `-` plus stdin, never a
  positional token (unchanged). The working directory is the spawn's `cwd`
  option — **never** a `-C`/`--cd` argv token (D-020's lesson; `codex exec
  resume` does not even have the flag). No `--dangerously-bypass-*` token is
  ever emitted by a profile. `model_provider=openai` is D-020's decreed
  transport, and it must be passed explicitly precisely because
  `--ignore-user-config` drops the config file that used to supply it. The
  eight `--disable` names are a **denylist with a known ceiling** (the
  signed limit); write that in the header comment beside them, with the
  verification command (Order of work) that proves each one still names a
  live feature.
- **The claude argv ordering rule.** `--allowedTools` is variadic and
  terminal, so `POLICY_FLAGS` must stay last: any flag added to this profile
  goes **before** it. `--model` goes after `--output-format json`. Start:
  `["-p", "--output-format", "json", ...model, "--append-system-prompt-file",
  <file>, ...POLICY_FLAGS]`. Resume: `["-p", "--resume", <id>,
  "--output-format", "json", ...model, ...POLICY_FLAGS]`. A future flag
  appended after `--allowedTools` would be silently eaten as a tool name;
  write that in the header comment.
- **No env name is added.** `harnessEnv`'s ten-name allowlist
  (`packages/shim/src/harness/env.ts`) is unchanged and untouched. This was
  measured, not assumed (Intent), for the default login location only. If
  the builder finds any part of this policy needs an eleventh name, **stop
  and report it as a finding** — it is an amendment to W-049's ruling and
  belongs to the architect and the Patron, never to a silent edit.
- **Checked by review, not by tests** (W-044/W-049 precedent): the codex
  policy constant is module-local, not exported from `harness/index.ts` or
  the shim index, and not configurable by a caller. A violation is a review
  finding.
- **Carried from W-049:** every codex spawn bisellium makes goes through
  `codexProfile`. After this opus, a talk-shaped codex dispatch has a CLI
  path (`bisellium talk --harness codex`), and the orchestrator's own
  `codex exec` shell calls are the interim transport D-020 retires for that
  shape — and only that shape.

## Behaviours to test

All eight live in a new `packages/cli/src/provider-portability.test.ts` —
1–6 from round 1, 7 and 8 added by the censor's round-1 review, and 7
re-specced by round 2's (its scenario shapes, not its subject: the count
stays **eight**). It imports
only exports that exist at HEAD — `claudeCodeProfile` and `codexProfile`
from `@bisellium/shim`, `runTalk` from `./talk.js` — so every red is
assertion-level, never a module-load error. Its `BISELLIUM_ONLY_BEHAVIOUR`
block harness is copied from `lifecycle.test.ts`; block N is behaviour N.

**Stubs.** Node stubs named `claude` and `codex`, written into a temp bin
dir per scenario and selected with `PATH` (W-049's pattern: a scenario must
not travel in an env var, because the env is scrubbed). Each stub exits 0 on
`--version`/`-V`, and on every other call appends one JSON line —
`{bin, args, stdin, cwd}` — to a log path that is **a literal baked into the
stub's source text**, then prints a canned envelope. A stub never executes
what it is handed. Two stub fidelity rules, both mirroring measured vendor
behaviour: the codex stub exits 2 with `unexpected argument` if a `resume`
argv contains `-s`, `--sandbox`, `-C` or `--cd`; the claude stub exits 1
with `unrecognized_model` if `--model` names a model that is not
`claude-*`. Canned envelopes: claude prints
`{"session_id":"sess-w046","result":"ack","is_error":false,"model":"claude-opus-5-20260101"}`;
codex prints JSONL with `thread.started` (`thread_id: "thr-w046"`), one
`item.completed`/`agent_message`, and a `turn.completed` carrying usage —
deliberately with **no** `model` key anywhere, which is what the real CLI
does.

Behaviours 1, 2, 4 and 5 drive `runTalk` against a temp copy of
`examples/sample-studio` with `--model-only` (the deterministic query path
must not swallow the turn). `eng-lead` there is `claude-opus-5`.

1. **The claude session spawns carry the decreed model, in the right slot.**
   `runTalk --sella eng-lead --model-only` runs twice against the temp
   fixture (a start, then a resume of `sess-w046`). Both logged argvs
   deep-equal the literal expected arrays above with
   `["--model","claude-opus-5"]` in the model slot, and in both the
   `--allowedTools` group is terminal and its ten values are exactly W-044's
   list. A third assertion pins the ordering rule directly: the index of
   `--model` is lower than the index of `--allowedTools`.
   - At HEAD this fails: the argv carries no `--model` at all, so the
     deep-equal and the index assertions both fail on their own line.
2. **The codex session spawns carry the decreed model and the whole codex
   command policy.** `runTalk --sella eng-lead --harness codex --model-only`
   runs twice. The start argv deep-equals
   `["exec", …POLICY, "-m", "claude-opus-5", "-"]` and the resume argv
   deep-equals `["exec","resume","thr-w046", …POLICY, "-m","claude-opus-5",
   "-"]`, with POLICY written out literally in the test — all eight
   `--disable` pairs included (the test is the spec, not a mirror of the
   module). Four further assertions, each its own line, hold on **both**
   argvs: no token is `-C` or `--cd`; no token starts with
   `--dangerously-bypass`; the logged `cwd` is the studio root; the logged
   `stdin` is non-empty and ends with the message.
   - At HEAD this fails: the argv is
     `["exec","--json","--skip-git-repo-check","-"]`, so every policy token
     and the model are missing.
3. **A turn that exits 0 with no reply is a failure, and nothing is
   persisted.** Two scenarios, one per vendor, each on its own fresh fixture
   copy and its own stub dir: a claude stub printing a valid envelope whose
   `result` is `""`, and a codex stub printing `thread.started` plus
   `turn.completed` and **no** `agent_message`. Both exit 0. For each, four
   assertions: `runTalk` returns a non-zero exit code;
   `sessions/<sella>.json` does not exist; `timeline/<sella>.jsonl` does not
   exist; and **`receipts/<sella>/` contains no receipt for the turn**
   (Sol finding 3 — `performTalk` writes three artefacts, so proving two
   absent proves nothing). The message printed on stderr names the sella,
   the harness and the requested model.
   - At HEAD this fails: `runTalk` returns 0, the session file, both
     timeline lines **and** the receipt are written, and an empty reply is
     printed as success.
4. **The decreed model is what the timeline records, and a vendor-echoed
   model wins.** On the codex stub (no model in any event), the `out`
   timeline entry's `model` is `"claude-opus-5"` — the requested value. On
   the claude stub (which echoes `claude-opus-5-20260101`), the `out`
   entry's `model` is `"claude-opus-5-20260101"` — the vendor's answer, not
   the request, because that is the truth about what ran. The `in` entry
   carries no model in either case.
   - At HEAD this fails on the codex half: `model` is absent from the entry
     (no model was ever requested and codex echoes none). The claude half
     passes at HEAD, and is asserted so the fix cannot be "always overwrite
     with the request".
   - Round 2 note (F-3): the echoing claude here is the **stub**. The real
     claude 2.1.280 echoes no `model` either, so live it is the fallback
     that runs on both harnesses. The stub half stays exactly as specced —
     it is the only way to pin "a vendor answer wins over the request", and
     that property must survive a vendor version that starts echoing again.
5. **One fixture sella, a talk turn on each harness, in one run.** Against
   a single temp fixture copy: `runTalk --sella eng-lead --model-only`
   (claude stub on `PATH`), then `runTalk --sella eng-lead --harness codex
   --model-only` (codex stub on `PATH`). Both return 0. Then, asserted as
   separate lines:
   - `sessions/eng-lead.json` ends pinned to `codex` with
     `sessionId: "thr-w046"` and `turns: 1` — the harness switch started
     fresh and no claude session id ever reached a codex argv.
   - `timeline/eng-lead.jsonl` has exactly four lines, and both `out` lines
     carry a model (`claude-opus-5-20260101` then `claude-opus-5`).
   - Exactly two receipts exist under `receipts/eng-lead/`, one naming each
     harness.
   - **Context crossed both harnesses.** The boot bundle's first non-empty
     line appears in the claude call's `--append-system-prompt-file` file
     contents, and in the codex call's logged `stdin`. (The claude stub
     copies the file's contents into its log line; the file is deleted after
     the call, so reading it later is not an option.)
   - Each call's argv carries its own harness's model and policy — the same
     two literal checks as behaviours 1 and 2, asserted here on one run.
   - At HEAD this fails: neither argv carries a model, and the codex `out`
     entry has none.
6. **The documented contract names the codex policy and its limits.**
   Sol finding 6: the owned doc change needs a runnable check, not a
   promise. The test reads `docs/ADOPTION.md`, takes the "Running talk"
   section (from its heading to the next `## `), and asserts it contains
   each of the following as a literal, one assertion per item so a red names
   exactly what is missing:
   - the policy tokens `--ignore-user-config`, `--ignore-rules`,
     `sandbox_mode=read-only`, `model_provider=openai`, and each of the
     eight `--disable` feature names;
   - that the decreed model comes from `sellae[].model` and that `talk` has
     no `--model` flag;
   - that codex's policy is **not** equivalent to the claude profile's, and
     each residual by name: no tool allowlist (a feature denylist that goes
     stale), unbounded reads, `AGENTS.md` still loads, and configuration
     channels beyond the user config file unmeasured;
   - that codex auth works only from `$HOME/.codex/auth.json`, and that
     `CODEX_HOME`/`OPENAI_API_KEY` are deliberately not passed.
   - At HEAD this fails on every item: the section documents neither a model
     nor any codex policy.
   - **Round 2: this block is not re-opened.** `bisellium red` writes
     `ci/reds/W-046/06.log` by behaviour number and **overwrites**
     (`packages/commands/src/lifecycle.ts:614`), so re-running it would
     destroy round 1's recorded evidence. The egress literal F-1 requires is
     therefore its own numbered block with its own fresh red — behaviour 8.
     Behaviour 6's assertion list is unchanged.

### Round 2 (censor F-1, F-2). Both ship as one change

The censor's instruction is explicit and this brief adopts it: **dispatch
behaviours 7 and 8 as one change, not two.** They are one class — the shipped
artefacts assert things about codex the live vendor refuted — and splitting
them would put two builder rounds on one review's findings.

### Round 3 (censor F-4). All three instances ship as one change

Behaviour 8 is closed and behaviour 7's scenarios B, C, F and G stand.
What round 3 changes is behaviour 7 alone, and it changes it as **one
class, not three instances**: I-1 (a parsed `is_error` envelope), I-2
(claude spawn failure) and I-3 (codex spawn failure) are all "a `Turn.raw`
shape the shared reader does not know about", and they close with one
widened read plus one capture fix. Fixing them one at a time is the W-026
pattern the censor's charter names — a round each. Scenario A's re-spec,
scenarios D and E, the widened read and the `claude-code.ts` line are
**one commit**.

7. **A failed turn's message carries the vendor's own diagnostic.**
   **CORRECTED, round 2 (censor F-4).** Round 2's preamble diagnosed one
   path and the fix was built to it, faithfully: it said `turn.reply` is
   `""` "on exactly the paths where the vendor said something useful —
   `claude-code.ts` puts `{stdout, stderr}` in `raw` when no envelope
   parses". That is **one** of four `Turn.raw` shapes. `reply` is *also*
   forced to `""` at `claude-code.ts:147` on a **parsed** `is_error`
   envelope, where `raw` is the envelope itself and carries neither a
   `stdout` nor a `stderr` key — and that is the shape the real vendor
   emits for the case this finding was named on. The censor enumerated all
   eight `raw:` constructions; the architect re-measured the live shape
   before amending (Intent). Corrected diagnosis: **`performTalk` must read
   the shapes the profiles actually produce, and the claude profile must
   stop discarding a stream it already captured.** Still one guard at one
   shared site, both profiles, never per call site.
   - **Measured, claude 2.1.280, streams separated, model mismatch:** exit
     1; **stdout is a valid 1284-byte envelope** — `is_error: true`,
     `api_error_status: 404`, `subtype: "success"`, `error` key **absent**,
     `result`: "There's an issue with the selected model (gpt-6-astra). It
     may not exist or you may not have access to it. Run --model to pick a
     different model."; **stderr** (78 bytes):
     `[claude-code:unrecognized_model] {"model":"gpt-6-astra","query_source":"sdk"}`.
     Both are useful and they say different things, so the seam carries
     **both** — see "the widened read" below. Contrast, measured: a resume
     against a vanished session id writes **0 bytes** to stdout and the
     diagnostic to stderr, which is why that symptom was already fixed.
   - **Scenarios.** Seven, each its own fresh fixture copy and stub dir,
     each assertion its own line. A, B, C, F and G keep round 2's shapes;
     **A is re-specced and D/E are new.**
     - **A — claude model mismatch (RE-SPECCED, F-4b).** Round 2 modelled
       this as "nothing on stdout, error on stderr", which is not what the
       vendor does and is why the defect passed review. The stub now emits
       the **measured** shape: exit 1, a **parseable** envelope on stdout
       carrying `is_error: true`, `api_error_status: 404`, `subtype:
       "success"`, no `error` key and the `result` sentence above, **and**
       the `[claude-code:unrecognized_model] …` line on stderr. Assert that
       the printed message carries a substring of **both** — the envelope's
       `result` sentence and the stderr line.
     - **B — codex bad provider.** Unchanged: exit 1, nothing on stdout,
       `Error: Model provider \`zzbogus\` not found` on stderr.
     - **C — claude vanished resume session.** Unchanged: exit 1, **0 bytes**
       on stdout, `No conversation found with session ID: sess-w046` on
       stderr, driven through the resume path.
     - **D — spawn failure, claude (NEW, F-4c: instance I-2).** The stub
       binary is absent from the stub `PATH` after `available()` has already
       returned true, so `spawnSync` fails and the profile returns
       `exitCode: 127`, `raw: { error: <message> }`. Assert the message
       carries a substring of that `error` string. (If making the spawn fail
       deterministically after a successful probe proves impossible in-test,
       call `claudeCodeProfile.start` directly with a `PATH` holding no
       `claude` and assert on the returned `Turn` plus a direct
       `vendorDiagnostic`-equivalent assertion through `runTalk` with an
       unavailable-harness stub — and say so in the handoff rather than
       dropping the instance.)
     - **E — spawn failure, codex (NEW, F-4c: instance I-3).** The same
       shape from `codex.ts`'s `child.on("error")` path: `exitCode: 127`,
       `raw: { error: <message> }`. Same assertion. D and E are the same
       shape in both profiles and close with the same read — they are
       separate scenarios so a one-profile fix cannot pass.
     - **F — redaction.** Unchanged.
     - **G — truncation.** Unchanged.
   - **The widened read, exactly** (one function, `vendorDiagnostic(raw)` in
     `packages/commands/src/talk.ts`; **no per-profile duplication**).
     Collect, in this fixed order, each value that is a non-empty string:
     1. `raw.stderr`;
     2. `raw.result`, **only when `raw.is_error === true`**;
     3. `raw.error` — the string itself, or its `.message` when it is an
        object (`ClaudeJsonResult.error` is typed `string | {message?}`).

     If that collection is empty, fall back to `[raw.stdout]` (today's
     fallback, unchanged). Join the collected parts with `" · "`, then
     redact and truncate to the **single existing 300-character budget** —
     one budget for the joined string, not one per part. When
     `raw.is_error === true` and `raw.api_error_status` is a number or
     string, prefix the joined text with `status <value>: `. No
     deduplication, no ordering by content, no emptiness test on anything
     but "is this a non-empty string".
   - **One doc assertion, in this block** (not behaviour 8, whose log is
     valid and unmoved): the "Running talk" section's sentence about a
     mismatched model names **both** what the operator actually receives —
     the envelope's own error text and the vendor's stderr line — and
     promises nothing the scenario-A assertions do not check. Round 2's
     shipped sentence promised `[claude-code:unrecognized_model]` while the
     live message carried neither string; at round 3's HEAD this assertion
     fails on its own line.
   - **Carry this note verbatim, so the fix is not over-complied with**
     (the censor's words): *reading `result` because the envelope's
     structured `is_error` flag is set is not "branching on the
     diagnostic" — the brief forbids branching on the diagnostic's TEXT,
     and `claude-code.ts:147` already branches on `is_error` itself.* The
     same holds for `api_error_status`: a status code is structured
     metadata, not the diagnostic's prose.
   - **One line in the profile, because a captured stream is being thrown
     away.** `claude-code.ts`'s parsed-envelope return (`raw: parsed`) is
     the only place either profile discards something it already has: the
     stderr line never reaches `raw` at all, so no reader at the shared site
     can recover it. Change that return to carry it — `raw: { ...parsed,
     stderr }` **only when `stderr` is non-empty and `parsed` has no own
     `stderr` key** (never overwrite a vendor field; do not infer from
     today's 25 keys that it will never ship one — that inference is what
     F-1 cost). `reply`, `exitCode`, `model` and `usage` on that branch are
     untouched, and `codex.ts` needs nothing: both its non-zero returns
     already carry `stderr`. This is a capture fix in one profile, not a
     second reader; the reading and formatting stay at the single shared
     site.
   - **Assertions**, for each scenario: `runTalk` returns non-zero; the
     stderr it printed contains the sella, the harness, the requested model
     **and a non-empty substring of the vendor's own diagnostic**; nothing
     is persisted (session, timeline and receipt all absent, as behaviour 3
     requires); and the message is passed through the same `redact` the
     timeline uses, asserted with a stub whose stderr carries a
     secret-shaped token that must **not** appear in the printed message.
   - **Bounded, deliberately:** the carried text is truncated to a stated
     character budget and is never parsed, matched or branched on. It is a
     diagnostic for a human, not a signal — a profile that starts deciding
     anything from stderr text is the class `claude-code.ts`'s header
     already forbids for usage-limit detection.
   - **At round 3's HEAD this fails on an assertion, not a load.** Scenarios
     B, C, F and G pass (round 2 shipped them and they are unchanged).
     Scenario A's two new assertions fail — the message is
     `art-lead: claude-code exited 1 (requested model: gpt-6-astra)` and
     carries neither the `result` sentence nor the stderr line, because
     `raw` is the envelope and `vendorDiagnostic` reads only
     `stderr`/`stdout`. Scenarios D and E fail the same way on the `error`
     key. Six failing assertion lines, every one of them about the widened
     read; the exit-code and nothing-persisted assertions pass throughout.
   - **Re-recording `07.log` is authorized for round 3, and only `07.log`.**
     The behaviour's certified scenarios changed, so the round-2 log no
     longer describes what is being claimed and a stale log would be the
     misrepresentation the red store exists to prevent. Record it with the
     same command and the same `--behaviour 7`, which overwrites
     `ci/reds/W-046/07.log` in place. **Do not re-run `red` for any other
     behaviour** — 01–06 and 08 are unchanged and their logs are evidence
     for claims that did not move. Note in the handoff that 07 was
     re-recorded, with the round-2 verdict commit as the reason, so the
     censor can see the replacement was authorized rather than quiet.
8. **The documented contract names the egress residual.** F-1's documentary
   half, numbered separately because behaviour 6's red is already recorded
   and `bisellium red` overwrites by behaviour number. Same mechanism as
   behaviour 6 — read `docs/ADOPTION.md`'s "Running talk" section, and
   `packages/shim/src/harness/codex.ts`'s header comment — and assert, one
   line each:
   - both texts state that a vendor-side web fetch/search channel survives
     the policy and is closed by none of the eight `--disable` names;
   - neither text contains an unqualified "no browser … feature" style claim
     that reads as "no egress": the achievement line names the eight feature
     flags and stops there;
   - both texts pair the egress residual with unbounded reads, naming W-044's
     refusal of that pairing.
   - At round 2's HEAD this fails: the shipped paragraph
     (`docs/ADOPTION.md`) enumerates four residuals and none is egress, and
     it claims "no browser, in-app-browser, apps or plugin feature". Two
     assertion-level failures, and it is `08.log`.

**No flag is invented.** Neither behaviour adds an egress-closing token to
the policy: none was probed, and a flag written into a constant on the
strength of a guess is exactly the inference that produced F-1. Whether one
exists is the follow-on's first question, beside the command-allowlist one.

## Test hardening (not a behaviour, so no red)

`talk-boundary.test.ts`'s `expectedStartArgv`/`expectedResumeArgv` gain the
model slot: absent for behaviour 1's direct profile calls (which pass no
model), `["--model","claude-opus-5"]` for behaviour 4's `runTalk`/`runTick`
calls against the fixture. Numbering this would force a fabricated red —
the W-044 round-3 provenance fault. Its evidence is the censor's mutation
run, and the step ordering below.

## Order of work, and where each red comes from

Work from the opus worktree root. Before trusting any run, check
`readlink -f node_modules/@bisellium/shim`: if it resolves to the main
checkout you are measuring the wrong file.

- **Step 0: re-verify the vendor facts this brief is built on.** Cheap, no
  model call. Round 1's wording said the results "go in the build log",
  which names no artefact this officina defines and was therefore verifiable
  by nobody (censor, F-4). The results go in **the builder's `bisellium
  handoff --next` note on the opus record**, and the gate is that **the
  censor re-runs these three commands and records their output in
  `studio/ci/W-046-review-<n>.log`** — which is what round 1's censor in
  fact did, so it is the precedent as well as the rule. Neither the builder
  nor the censor hand-writes a `ci/*.log` (CLAUDE.md, "Never by hand"):

      codex --version
      codex exec --help > /tmp/exec.txt; codex exec resume --help > /tmp/resume.txt; diff /tmp/exec.txt /tmp/resume.txt
      codex features list --disable browser_use --disable browser_use_external --disable browser_use_full_cdp_access --disable in_app_browser --disable apps --disable plugins --disable remote_plugin --disable plugin_sharing

  The diff is how the start-only/resume-only divergence is established for
  the installed version rather than trusted from this brief (the inventory
  here is explicitly non-exhaustive). The `features list` run must show all
  eight names as `false`; a name that no longer exists, or that is already
  `false` by default, is reported before it is put in the constant.
  **What step 0 cannot tell you** (F-1, learned the expensive way): the
  feature table is not the tool surface. A feature listed as removed,
  deprecated or `false` does **not** license a claim that the corresponding
  tool is absent. Only a live probe of the tool itself does, and no such
  claim may be written into the policy, the docs or this brief without one.
- **Step 1: reds.** Write `provider-portability.test.ts` and add it to
  `package.json`'s `test`. Nothing else. Record each red:

      BISELLIUM_ONLY_BEHAVIOUR=<n> npm run -s bisellium -- red W-046 --behaviour <n> --repo . --studio studio -- node --import tsx packages/cli/src/provider-portability.test.ts .

  for n = 1 to 6, and confirm the six logs differ. Check each log for
  leakage before committing: no real session id, no real usage number, no
  env value. Commit the test and the reds.
- **Step 2: the claude half plus the talk-boundary update.** `types.ts`,
  `claude-code.ts`, `talk.ts`'s model plumbing, and
  `talk-boundary.test.ts`'s model slot. Behaviours 1 and 4's claude half go
  green; `talk-boundary.test.ts` and `harness-env.test.ts` stay green. Land
  it as its own commit, so the censor can see that adding a flag to the
  claude argv did not move W-044's boundary.
- **Step 3: the codex half.** `codex.ts`'s policy constant and `-m`, and
  `performTalk`'s no-reply refusal. Behaviours 2, 3, 4 and 5 go green.
- **Step 4:** `docs/ADOPTION.md` (behaviour 6 goes green), the header
  comments, and `studio/bisellium.yml`'s `harness: codex` line.
- **Step 5 (round 2, after censor review 1): behaviours 7 and 8, one
  change.** Steps 1–4 are `done` and committed; the tree is green on
  behaviours 1–6. Record `07.log` and `08.log` **first**, from the same
  harness as before:

      BISELLIUM_ONLY_BEHAVIOUR=<n> npm run -s bisellium -- red W-046 --behaviour <n> --repo . --studio studio -- node --import tsx packages/cli/src/provider-portability.test.ts .

  for n = 7 and 8, then implement: `performTalk`'s diagnostic guard, and the
  two texts (`docs/ADOPTION.md` and `codex.ts`'s header). Do **not** re-run
  `red` for behaviours 1–6: it overwrites `ci/reds/W-046/<nn>.log` and
  would destroy round 1's evidence. Do **not** edit an existing red log.
- **Step 6 (round 3, after censor review 2): behaviour 7's three instances,
  one change.** Behaviour 8 is closed; behaviours 1–6 are green and their
  logs stand. Order, and it matters:
  1. Re-spec the test first — scenario A's stub to the measured
     `is_error`-envelope shape, new scenarios D and E, and the one doc
     assertion. Nothing in `talk.ts` or `claude-code.ts` yet.
  2. Re-record **`07.log` only**, same command, `--behaviour 7`. It
     overwrites round 2's log in place, which is authorized this round and
     only for this log, because the behaviour's certified scenarios changed
     (behaviour 7's own entry says so). Confirm the new log shows scenarios
     B, C, F, G passing and six failing assertions across A, D and E — a
     log where A still passes means the stub was not re-specced.
  3. Then implement, as one commit: `vendorDiagnostic`'s widened read,
     `claude-code.ts`'s `raw: { ...parsed, stderr }` guard, and the
     `docs/ADOPTION.md` sentence.
  Do **not** re-run `red` for 01–06 or 08. Do **not** edit any red log by
  hand. Record in the handoff that 07 was re-recorded and why, naming the
  round-2 verdict commit — a replacement the censor has to discover is
  indistinguishable from one that was hidden.

If any other existing test breaks, stop and report. It means the argv moved
further than this brief says.

## Acceptance

- `npm test` is green, and `npm run -s typecheck`, `npm run -s lint` and
  `npm run -s format:check` are clean.
- `studio/ci/reds/W-046/01.log` … `08.log` exist, each with a non-zero exit
  and a distinct body. `01`–`06`'s `at` is earlier than the first commit
  touching a file under `packages/shim/src/` or `packages/commands/src/`;
  `08`'s `at` is earlier than the round-2 commit that implements it and
  **later** than the round-1 verdict — a round-2 red recorded before round
  1's review log is backfilled evidence, not a red. **`07` is re-recorded in
  round 3** (behaviour 7's scenarios changed; authorized there and nowhere
  else): its `at` is later than the **round-2** verdict commit and earlier
  than the round-3 implementation commit, and its body shows scenarios B, C,
  F and G passing with six failures across A, D and E. `01`–`06` and `08`
  are byte-identical to what rounds 1 and 2 recorded — any movement in them
  is a finding, not a refresh.
- Step 0's three outputs are in the builder's handoff note and re-run by the
  censor into `studio/ci/W-046-review-<n>.log`, and the eight `--disable`
  names in the shipped constant match the ones step 0 showed flipping.
- `npm run -s check -- examples/sample-studio --repo .` passes.
  `npm run -s check -- studio --repo .` reports no new block; the one
  expected change is that `hook.dead` for `builder-sol` disappears (Files
  owned). Any other movement is a finding.
- `npm run -s bisellium -- verify W-046 --studio studio --repo .` on
  `opus/W-046`, clean tree.
- **The censor's mutation run** (kept in the review log, never in
  `ci/reds/`). Every one of these must fail the suite:
  - Drop `--model` from claude `start` only; then from `resume` only.
  - Drop `-m` from codex `start` only; then from `resume` only.
  - Move `--model` after `--allowedTools` in the claude argv.
  - Drop `--ignore-user-config`; then `--ignore-rules`; then
    `-c sandbox_mode=read-only`; then `-c model_provider=openai`; then each
    `--disable` pair — one at a time.
  - Emit `-s read-only` instead of `-c sandbox_mode=read-only` on `resume`
    (the stub exits 2 on it, as the real CLI does).
  - Add a `-C <cwd>` token to either codex argv.
  - Read the model from `process.env` instead of the manifest row.
  - Overwrite `Turn.model` with the requested model unconditionally.
  - Accept an exit-0 empty-reply turn as success, for each vendor.
  - On a refused turn, write the session only; then the timeline only; then
    **the receipt only** — each must fail behaviour 3.
  - Resume a codex session with a claude session id (behaviour 5's switch).
  - Delete any one required claim from the `docs/ADOPTION.md` paragraph,
    including one residual from the signed limit — behaviour 6 must fail.
  - **Round 2.** Drop the vendor diagnostic from the failure message; carry
    it on the claude path only; carry `raw` un-redacted (the secret-shaped
    token must not reach stderr); carry it without truncation; branch on its
    text instead of printing it — each must fail behaviour 7. Delete the
    egress residual from `docs/ADOPTION.md`; delete it from `codex.ts`'s
    header; restore the round-1 "no browser … feature" achievement line —
    each must fail behaviour 8.
  - **Round 3.** Read `result` regardless of `is_error` (it must be gated on
    the flag, not taken whenever present); drop the `result` read and keep
    only `stderr`; drop the `error` read; close I-2 without I-3 (claude
    spawn shape only) and then I-3 without I-2; drop the `claude-code.ts`
    stderr-preservation guard so `raw` is the bare envelope again; make that
    guard overwrite an existing `stderr` key on `parsed`; give each
    collected part its own 300-character budget instead of one budget for
    the joined string — each must fail behaviour 7. Restore round 2's
    scenario-A stub shape (nothing on stdout) with the fix in place: the
    suite must stay green, since that stub models a shape the vendor also
    produces — but the round-3 **red** must have failed with it re-specced,
    which is what the re-recorded `07.log` shows.
- **One attended live dual-harness smoke, after the fix, as part of the
  censor's review** (D-014). This is the seam's live proof and the only
  place real vendors run. On a **temp copy of `studio/`** (`cp -r studio
  <tmp>`), never `studio/` in place, with the operator logged in to both
  vendors under `HOME`:

      npm run -s bisellium -- talk --sella eng-lead    --model-only --studio <tmp> "Name the opus you are booted for and stop."
      npm run -s bisellium -- talk --sella eng-lead    --model-only --studio <tmp> "Name it again in three words."
      npm run -s bisellium -- talk --sella builder-sol --model-only --studio <tmp> "Name the opus you are booted for and stop."
      npm run -s bisellium -- talk --sella builder-sol --model-only --studio <tmp> "Name it again in three words."

  Pass conditions, recorded in the review log:
  - All four exit 0 with a real reply. The second call of each pair is a
    **resume** (the session file exists), so this is the live proof that
    `--model` is accepted and honoured on a resumed session on both
    vendors — Sol finding 5, the case stub equality cannot reach.
  - **CORRECTED, round 2 (F-3).** `<tmp>/timeline/eng-lead.jsonl`'s `out`
    model is **exactly `claude-opus-5`** on both turns, and
    `<tmp>/timeline/builder-sol.jsonl`'s is exactly `gpt-5.6-sol` on both —
    the decreed values, arriving through the `turn.model ?? requested`
    fallback. Round 1 asked for a "build id"; neither installed vendor emits
    one (claude 2.1.280 has no `model` key at all, and `modelUsage` holds
    only the alias), so that condition was unmeetable by any implementation
    and is struck. A dated build id appearing here would mean a future
    vendor started echoing, which the fallback already handles and
    behaviour 4 already pins.
  - `<tmp>/sessions/builder-sol.json` has `harness: "codex"`, a non-null
    `sessionId`, and `turns: 2`.
  - **The claude resume turn: already inconclusive, round 1, and it stays
    that way.** The path worked as designed and needs no change. Round 1's
    censor ran it, hit "No conversation found with session ID" (exit 1),
    reproduced it identically on `master`, and isolated the `--model` token
    as innocent by an A/B argv run. So: a **pre-existing** defect in talk's
    claude resume path, filed as its own opus by the orchestrator, not a
    failure of W-046 and not W-046's to fix. Round 2's censor does not
    re-litigate it — it re-runs the four turns, and if the claude resume
    fails the same way it is recorded as inconclusive again, with the exit
    code and the opus id of the filed defect. The codex resume half still
    must pass. If the claude resume ever *passes*, that is the live proof
    round 1 could not get, and it is worth a line in the review log.
    (Note for the filed opus: `performTalk` refuses a failed turn before it
    touches the session file, so a resume against a vanished session id is
    a hard stop, not a silent restart. Whether it should fall back to
    `start` is that opus's question — and behaviour 7 is what makes the
    vendor's reason visible enough to decide it.)
  - **The codex policy is enforced, live.** A further codex turn asking for
    a write (`touch <tmp>/zz-w046-smoke.txt`, reporting only the exit code)
    reports a non-zero exit and leaves no such file. The architect measured
    this on 2026-09-24 on both `exec` and `exec resume`; the censor
    re-measures it through `talk`.
  - **The residuals are recorded, not waived.** One codex turn asking the
    session to list the tools it has; the reply goes in the review log as
    the baseline for the follow-on opus. It is **not** a pass condition —
    a model's self-report is not evidence of a boundary — and it must not
    be written up as one.
  - **The mismatch fails closed, readably — the round-3 condition, stated
    against the measured envelope.** `talk --sella art-lead --model-only
    --studio <copy of examples/sample-studio>` exits non-zero and the
    message names the sella, the harness, the model (`gpt-6-astra`) and
    **both** diagnostics the vendor produced for that turn: a substring of
    the envelope's `result` sentence ("There's an issue with the selected
    model (gpt-6-astra)…") **and** the stderr line
    (`[claude-code:unrecognized_model]`). Round 1 asserted only "names the
    model" and got neither; round 2 named the stderr string alone, which
    `raw: parsed` had already discarded, so the condition was unmeetable
    without the capture fix. The censor quotes the actual message in the
    review log rather than paraphrasing it. No session, timeline or receipt
    file is written.
  - **The egress residual is re-measured, not taken from this brief.** One
    codex turn through `talk` asking for a live fetch of a value that moves
    (the censor's round-1 control was `api.github.com`'s
    `stargazers_count`), with the answer compared against the censor's own
    independent fetch. Recorded either way. If it ever comes back
    `NO-NETWORK` under the shipped argv, that is a finding **for** the
    follow-on (something closed it) and the docs' residual is re-opened by
    measurement — not by inference.
  - If either vendor is not logged in under `HOME`, or auth is not the
    default `$HOME/.codex/auth.json` file login, the result is
    "inconclusive", not a pass. Log in and rerun.

## Out of scope

- **One opus end-to-end on both providers.** The originating requirement
  (`docs/research/agent-studio-landscape-2026-09-21.md`,
  `docs/research/herdr-landscape.md`), minus what this opus delivers. It
  needs build-shaped dispatch: a session with write permission, a worktree,
  a bounded task, and a real command allowlist on the codex side. File it as
  *"one opus end-to-end on both providers: bisellium dispatches a bounded
  build on either harness"*, and give it the **three** questions this opus
  could not answer — how a codex session gets an enumerated command
  allowlist (execpolicy `.rules` is **not** known to be the answer); what a
  write-policy envelope field looks like; and **whether any flag closes the
  live web fetch/search egress** the censor measured (F-1), which no probe
  in this opus attempted. Until it lands, builders are spawned by the
  vendor's Agent tool and codex builds stay orchestrator-mediated (W-050 is
  the decreed trial, on that transport).
- **Closing codex's residual configuration and tool surfaces, egress
  first.** Enumerated in the signed limit and repeated in
  `docs/ADOPTION.md` and `codex.ts`'s header. Closing them is not deferred
  out of laziness: egress, `AGENTS.md`, project/managed/system config and
  read enumeration each need their own measurement, and none has a flag in
  `codex exec --help` known to close it on 0.153.4. They belong with the
  follow-on, where a write-capable session makes them load-bearing — and
  egress is where it starts, because it is the half of W-044's refused
  pairing that a single flag might close.
- **Closing stdin on the codex version probe.** Round 1 owned this; Sol
  finding 4 is right that it is an untested behaviour change with no
  measured failure behind it (`codex --version` exits without reading
  stdin, which is why `available()` works today). Dropped rather than
  numbered: it would need an EOF-waiting stub to test honestly, and that is
  a whole behaviour bought for a hypothetical.
- **The Herdr multiplexer socket-shape experiment.** Out. It buys nothing
  beside this seam: a talk turn is a synchronous spawn that either returns a
  Turn or fails, and the multiplexer's value is liveness, bounded retries
  and cross-provider recovery for dispatches that outlive one process. It
  becomes worth building the day the follow-on above lands and a dispatch
  can survive its parent — exactly the condition
  `docs/research/herdr-landscape.md`'s `kill_when` names.
- **Hooks on codex.** Hooks are Claude-only (`bisellium hooks print
  --harness claude-code`), and codex has its own hook-trust model
  (`--dangerously-bypass-hook-trust`, which a profile must never emit).
  Provider-neutral hooks are their own opus. Note that declaring
  `builder-sol` as codex removes its `hook.dead` finding (Files owned):
  that is correct reporting, and it is also a reminder that hook liveness
  evidence does not exist for a codex sella.
- **A `--model` flag on `talk`.** See Interfaces: models are decreed.
- **A check rule for model/harness coherence.** Tempting — "process
  enforcement is a check rule, never prose" — but it would have to match
  vendor name patterns (`claude-*`, `gpt-*`) and D-020 says tier holders
  churn by design. That is W-049's denylist lesson in a new costume: the
  rule goes stale, and a stale block is worse than a loud vendor error.
  **Round 2 (censor F-2c): this decision's premise had failed and is
  restored by behaviour 7, not waived.** Round 1 declined the rule because
  "the vendor already fails closed and names the model
  (`[claude-code:unrecognized_model]`)" and said "reopen only if the
  fail-closed message proves unreadable in practice" — and the censor then
  measured it unreadable (`art-lead: claude-code exited 1`, nothing else).
  The vendor does emit the string; bisellium was discarding it. Behaviour 7
  makes the operator see it, which is what the premise asserted all along,
  so the rule stays out of scope. If behaviour 7 is descoped for any
  reason, this entry reopens with it — they stand or fall together.
- **`examples/sample-studio`'s `art-lead` row** (`gpt-6-astra` on the
  default claude-code harness). Left as-is and filed: fixing it means
  either a model change or `harness: codex`, and the latter changes how many
  vendor calls `tick` makes in `talk-boundary.test.ts` behaviour 4 and
  `harness-env.test.ts` behaviour 4. That is a fixture opus with its own
  test updates, not a rider here. Every test in this repo drives stubs, so
  nothing goes red; only a real talk to that fixture sella fails, which is
  correct behaviour and is an Acceptance assertion above.
- **`run`.** It executes the operator's command in the operator's env and
  selects no harness (W-049 ruled the same way for the same reason).
- **The vendor's login shell re-pollution.** Codex runs model-generated
  commands through `/usr/bin/zsh -lc` (observed), which re-sources the
  operator's rc — the same residue W-049 left on the claude side and for the
  same reason: it belongs to the vendor, and this opus governs the vendor
  process's own env and argv.
- **`redact.ts`/`filterEnv`, `providers`, `verify`/`ci`.** Untouched.

## For Sol (red-team targets)

Round 1's six blocking findings are all accepted: 1 by rescoping and
renaming (this brief no longer claims an opus crosses both providers), 2 by
replacing "closes it" with a signed limit plus a verified `--disable`
denylist, 3 by asserting receipt absence, 4 by dropping the stdin change, 5
by putting a live claude **and** codex resume in the attended smoke with a
named inconclusive path, 6 by making the doc paragraph behaviour 6 with a
literal-by-literal assertion. Notes 7–11 are folded in ("no dispatch path
reads it"; the `hook.dead` consequence named; the divergence inventory
marked non-exhaustive with a step-0 diff that establishes it per version;
the auth-location qualification kept in the brief, the docs and the smoke).

**Round 2 amendment (censor review 1).** Sol's round-1 target list asked
the right question and got the wrong answer from me on one point: target 2
asked "which enabled-by-default feature did I miss", and the answer was
"the question was mis-framed" — the feature table is not the tool surface,
and a working `web.run`/`web_search` channel survives all eight `--disable`
names. F-1 is that miss, measured. Target 1 (is the signed limit a limit or
a permission slip?) is now the live question, because the limit was missing
its largest entry. The standing rule this produced, written into the brief
and into Order of work: **a residual enters the list by measurement and
leaves it only by measurement** — never by reading a flag table.

**Round 3 amendment (censor review 2).** Round 3's target 0 asked whether
"a non-empty substring of the diagnostic" was "a strong enough assertion or
one a single character satisfies". The real answer came from a different
direction: the assertion was strong enough, and the **stub** was wrong.
Scenario A modelled a claude mismatch as "nothing on stdout, error on
stderr"; the vendor emits a full parseable envelope on stdout *and* the
stderr line, so `raw` was the envelope and the shared reader found nothing
(F-4). Sol's round-1 target 5 asked exactly this question — "what does a
stub let pass that the real CLI would refuse" — and it was answered too
narrowly. The standing rule this produces, beside F-1's: **a stub's failure
shape is a measurement, not a guess; a scenario asserting a diagnostic
must model the streams the vendor actually writes, both of them.**

Round 4 targets, in the order I think they are most likely to be wrong:

0. **Behaviour 7's blast radius, now wider.** The message can carry an
   envelope's `result` prose, a stderr line, a spawn error, or a joined
   string of several — all redacted and bounded to one 300-character budget.
   Attack the bound: can a vendor's stderr or `result` carry something that
   must not reach a terminal or a CI log even redacted? Is joining with
   `" · "` inside one budget a silent truncation of the *second* part
   whenever the first is long — and if so, is that the right trade against
   a per-part budget? And is "a non-empty substring" still strong enough
   now that three sources can satisfy it?
0b. **The one profile-side line.** `raw: { ...parsed, stderr }` puts a key
   in `Turn.raw` that the vendor did not emit, on a field whose contract
   says callers must not assume a shape. I guarded it (only when stderr is
   non-empty, never overwriting an existing key) and kept the reading at the
   shared site. Is that a contract violation dressed as a capture fix, and
   would nesting (`{ envelope, stderr }`) be more honest at the cost of
   every existing reader?
1. **Is the signed limit actually a limit, or a permission slip?** I chose
   to ship a codex policy that is admittedly weaker than claude's and to
   write the gap down, rather than block until parity exists. F-1 makes this
   sharper, not softer: the profile ships with unbounded reads **and**
   working egress, the exact pairing W-044 refused. Attack the choice on its
   own terms — is documenting that pairing sufficient, or should codex talk
   be blocked until one half is closed? Unbounded reads including
   `~/.codex/auth.json` is the one I would
   attack first.
2. **The `--disable` denylist, after F-1.** Eight names, verified to flip in
   `codex features list` on 0.153.4 — and now known not to imply the
   matching tool is gone. The censor's live census found eighteen tools in a
   policy-applied session (`apply_patch`, `exec_command`, `write_stdin`, an
   image generator, a `collaboration.*` agent-spawning family, `web.run`).
   Which of those eighteen is a second F-1 — a capability the signed limit
   does not name and a probe would demonstrate? `collaboration.spawn_agent`
   is the one I would probe next: a talked sella that can spawn agents is a
   dispatch path bisellium does not own. Also still open: does `--disable`
   survive a **resume** in effect, or only in argv parsing? I proved it
   parses; nobody has proved a resumed session honours it.
3. **Behaviour 3's predicate.** "Exit 0 with an empty reply is a failure."
   Round 1 you agreed this fits talk's promise of a textual reply. Re-check
   it against the codex event shapes now that the policy disables tool
   features: is there a turn shape that legitimately produces only
   `command_execution` items and no `agent_message`, and would this rule
   turn it into an error?
4. **Behaviours 6 and 8 as doc tests.** Literal string matching over a prose
   section is brittle and can be satisfied by a paragraph that names the
   tokens while claiming the opposite — and behaviour 8 now asks for a
   **negative** literal too ("no unqualified 'no browser … feature' claim"),
   which is weaker still: a rephrasing evades it. Propose a sharper
   predicate if one exists that is not a full natural-language check.
5. **Stub fidelity.** The stubs now imitate two measured vendor refusals
   (codex `resume` rejecting `-s`/`-C`, claude rejecting a non-claude
   `--model`). What else does a stub let pass that the real CLI would
   refuse — argv order, repeated `--disable`, a `-c` value needing TOML
   quoting, an empty `-m`?
6. **The env claim, one more time.** No eleventh name, measured for
   `$HOME/.codex/auth.json` file login only. Find the operator setup where
   this policy needs one: a proxy-less machine, a device-code login, an
   auth refresh that writes somewhere else.
7. **Unenumerated classes.** Three: flags `codex exec` accepts that
   `codex exec resume` does not (step 0's diff is meant to close this per
   version — does it?); every place a model could enter a session other
   than the manifest row (env, config file, resumed session, vendor
   default); and every caller of `performTalk` (talk, `talkOnce`/tick,
   `serve`'s `POST /api/talk` — is there a fourth?).
8. **Acceptance a stub could satisfy.** Which Acceptance bullet would still
   pass if the implementation passed `--model` to the *version probe*
   instead of the turn, or built the policy array but never spread it into
   argv? If any would, name the missing assertion.
