# W-050 — bisellium ci enters the documentation: template edit, CLAUDE.md regenerated

Spec of record, signed by the architect, 2026-09-24. Docs-only. This is
D-020's **codex-build trial opus**: gpt-5.6-sol builds it, and the Opus-tier
censor judges it with its normal verdict. More than 2 review rounds reads
as a trial fail. Sol red-teams this brief (D-020) as input, not authority.
The `ready` gate is signed on `opus/W-050` after `bisellium branch` (D-021),
and not by the builder, because codex models sign no gates (D-020).

## Intent

`bisellium ci` (W-031, `packages/commands/src/ci.ts`) runs `CI_STEPS`, the
six commands `.github/workflows/ci.yml` runs, locally and without a runner.
Only the CLI usage banner mentions it. No documentation does:
`CLAUDE.md`/`AGENTS.md` (generated from
`packages/cli/src/instructions.template.md`) and `docs/ADOPTION.md` are
both silent about it.

`scripts/ci-workflow.test.mjs` is the drift guard between `CI_STEPS` and
the workflow. It enforces the 2026-09-20 split: the `gates` job runs
`CI_STEPS` minus the studio check, in order, and the `officina` job runs
the studio check alone. It also keeps the resulting order divergence on
purpose. None of this is written down anywhere a reader would look.

This opus adds one line to the template's Commands section and regenerates
`CLAUDE.md`/`AGENTS.md` with `bisellium instructions --write`. It adds a
`## Running ci` section to `docs/ADOPTION.md`, and extends the drift guard
so that the docs now drift *with* `CI_STEPS`, the workflow and `ci`'s
usage line, not apart from them.

**`docs/STUDIO.md` does not change.** It is the generic studio model
(vocabulary, collegia, cadence, autonomy) and has no command reference.
`ci` is this repository's own pipeline, not a model concept.

## Preconditions (orchestrator, before dispatch)

Codex models write no officina bookkeeping except through the CLI (D-020).
Everything below is done by the orchestrator, not by the builder.

1. **Declare the builder's seat. (Done: `builder-sol` is in
   `studio/bisellium.yml` as of 2026-09-24.)** `handoff` refuses an undeclared sella
   (`writes.ts`, `runHandoff`). The trial's evidence has to name the model
   that produced it. Borrowing `builder-a`…`builder-d` would attribute
   Sol's reds to a Sonnet seat. So, trunk-side and landed with this brief,
   add this line to `studio/bisellium.yml` directly after the `builder-d`
   line:

       - { id: builder-sol, collegium: engineering, kind: agent, model: gpt-5.6-sol }

   If the orchestrator declines, it amends this brief before dispatch. The
   builder never substitutes another id.
2. Land this brief and the seat line on the trunk. Then, from the trunk
   checkout, run
   `npm run -s bisellium -- branch W-050 --studio studio --repo .` and
   `git worktree add <WT> opus/W-050`.
3. In `<WT>`, run
   `npm run -s bisellium -- ready W-050 --sella architect --studio studio`,
   then commit `studio/opera/W-050.md` as
   `chore(W-050): spec gate signed on the branch per D-021`.
4. Dispatch Sol with `<WT>` as its working directory and the section
   "For the trial builder" below as its instructions.

## Files owned

- `scripts/ci-workflow.test.mjs`: new assertions (behaviours 1 and 2) and
  one comment block, appended after the last existing `check(...)` and
  before `process.exit`. Existing assertions are unchanged.
- `packages/cli/src/instructions.template.md`: one line added (below).
- `CLAUDE.md`, `AGENTS.md`: regenerated by `instructions --write`, never
  hand-edited. The same run rewrites `GLOSSARY.md`, which must come out
  byte-identical.
- `docs/ADOPTION.md`: one new section (below), and nothing else.
- Written through the CLI only: `studio/ci/reds/W-050/01.log` and `02.log`
  (`bisellium red`), and `studio/opera/W-050.md`'s `traditio`
  (`bisellium handoff`).

## Interfaces

### The template line (exact)

Insert this as a new line directly after the line
`` - `bisellium verify <opus> --studio studio --repo .` (on the opus branch) ``
in the template's `## Commands` section:

```
- `bisellium ci` (the CI workflow's steps, run locally; `--opus <id> --studio studio` then runs `verify`, on the opus branch)
```

Call this `CI_LINE` in the test.

### The ADOPTION section (exact)

Insert the following block into `docs/ADOPTION.md` directly before the line
`## Writing to a studio (handoff, emit, answer, greenlight, budget)`. That
places it after the last paragraph of `## Running run and verify`. Keep
exactly one blank line between the inserted block and that heading.

````
## Running ci

```bash
npm run bisellium -- ci [--ref <ref>] [--opus <id>] [--studio <dir>] [--repo <dir>] [--allow-dirty]
```

`ci` runs this repository's CI pipeline locally, without a runner: the
commands in `CI_STEPS` (`packages/commands/src/ci.ts`), in order, in
`--repo` (default the current directory), stopping at the first that
fails. Today those are `npm run -s typecheck`, `npm run -s lint`,
`npm run -s format:check`, `npm test`,
`npm run -s check -- studio --repo .` and
`npm run -s check -- examples/sample-studio --repo .`. The steps are fixed
in code and name this repository's two officinae; `ci` is not an adoption
feature. `.github/workflows/ci.yml` runs the same commands, each after
`npm ci`, split across two jobs: `gates` runs every step except the studio
check, in `CI_STEPS` order, and is the one a branch protection rule can
require; `officina` runs `npm run -s check -- studio --repo .` alone and is
deliberately not required, since it stays red on known officina debt
(W-028's ruling). So the runner does not reproduce `ci`'s single sequence,
and that divergence is deliberate. `scripts/ci-workflow.test.mjs`, part of
`npm test`, fails when they drift: when `gates` differs from `CI_STEPS`
minus the studio check, when `officina` is anything but that one check, or
when this section stops naming a `CI_STEPS` command or `ci`'s usage. The
workflow never runs `verify`, so no certificate is ever written from a
runner.

A failed studio check also prints that it is officina-wide and that
`bisellium verify <opus>` is the per-opus gate. `--ref <ref>` runs the
steps in a scratch worktree of `<ref>` instead, after a from-scratch
`npm ci` there against a throwaway cache (a worktree without its own
`node_modules` would resolve `@bisellium/*` through the parent checkout and
test the wrong code), and releases the worktree afterwards. `--opus <id>`
runs `bisellium verify <id>` against the same tree once every step has
passed, forwarding `--studio`, so the automated gates' certificates come
from `verify` itself; run it on the opus branch (D-021). `--allow-dirty` is
passed to that `verify`, and on the `--ref` path it always is, since the
scratch checkout is untouched. Exit codes: 0 every step passed (with
`--opus`, `verify`'s own exit code instead), 1 a step or `--ref`'s install
failed, 2 usage error, no git repo for `--ref`, or a ref that cannot be
checked out.
````

Each claim maps to `ci.ts`: `CI_STEPS`; `runStep` and the loop's early
return; `CHECK_STUDIO_STEP`'s hint; `defaultInstallDeps`; the `finally`
release; `verifyArgs` and the `allowDirty || ref !== undefined` branch; and
the exit codes of `runCi`. The job facts map to `ci.yml`'s own comments.
The guard facts map to `scripts/ci-workflow.test.mjs`.

### The new assertions (names exact; each is one `check(name, ok, detail)`)

Constants in the script:

- `VERIFY_LINE` = `` - `bisellium verify <opus> --studio studio --repo .` (on the opus branch) ``
- `CI_LINE` = the template line above.

Read each file with `readFileSync(join(REPO_ROOT, …), "utf8")` and split
it into lines with `.split(/\r?\n/)`. Always use `REPO_ROOT`, never the
`argv[2]` target: docs are always the real ones. Define
`norm = (s) => s.replace(/\s+/g, " ")`.

**Behaviour 1:**

- `instructions template: ci line follows verify line`. Take the lines of
  `packages/cli/src/instructions.template.md`. Let `i` be the index of
  `VERIFY_LINE`. `ok` is `i !== -1 && lines[i + 1] === CI_LINE`.
- `CLAUDE.md carries the ci line`. `ok` is true when some line of
  `CLAUDE.md` is exactly `CI_LINE`.
- `AGENTS.md carries the ci line`. The same test, on `AGENTS.md`.

**Behaviour 2.** Let `adoption` be the lines of `docs/ADOPTION.md`, and
let `h = adoption.indexOf("## Running ci")`. `section` is
`norm(adoption.slice(h + 1, end).join(" "))`, where `end` is the index
of the first line after `h` that starts with `"## "` (or
`adoption.length` if there is none). When `h === -1`, `section` is `""`.

- `ADOPTION.md: "## Running ci" follows "## Running run and verify"`.
  `ok` is `h !== -1`, and the nearest line before `h` that starts with
  `"## "` must be exactly `## Running run and verify`.
- `ADOPTION "Running ci" shows ci's usage`. Read
  `packages/commands/src/ci.ts` as text and match
  `/const USAGE = "usage: bisellium ci ([^"]*)";/`. `ok` is true when the
  match exists and `section` includes the plain string
  `"npm run bisellium -- ci " + m[1]` (no backticks: the usage sits in the
  bash fence). On no match, `ok` is false and the detail is
  `USAGE not found in ci.ts`.
- For each `step` of `ciSteps`, in order:
  `ADOPTION "Running ci" names step: ${step}`. `ok` is
  `section.includes(step)`.
- `ADOPTION "Running ci" names CI_STEPS, ci.ts and this guard`. `ok` is
  true when `section` includes each of these three JS strings, backtick
  characters included: ``"`CI_STEPS`"``,
  ``"`packages/commands/src/ci.ts`"`` and
  ``"`scripts/ci-workflow.test.mjs`"``.
- `ADOPTION "Running ci" names the gates and officina jobs`. `ok` is true
  when `section` includes the JS strings ``"`gates`"`` and
  ``"`officina`"``, backtick characters included.

Before these assertions, add one comment block that names W-050 and
explains why the docs are guarded here: this script already holds
`ciSteps` and the job split, so the docs drift with them.

## Behaviours to test

1. The instructions template's Commands section carries `CI_LINE`
   directly after `VERIFY_LINE`, and the committed `CLAUDE.md` and
   `AGENTS.md` both carry it, produced by `bisellium instructions --write`
   (the first three assertions above). Red: before any doc edit, all three
   FAIL because the line is absent.
2. `docs/ADOPTION.md` has a `## Running ci` section immediately after
   `## Running run and verify`. It shows `ci`'s usage as derived from
   `ci.ts`, names every `CI_STEPS` command verbatim, and names `CI_STEPS`,
   `ci.ts`, the guard and both job ids (the remaining assertions). Red:
   recorded after behaviour 1 is green, so the log shows behaviour 1's
   three checks PASS and every behaviour-2 check FAIL. That body cannot
   equal behaviour 1's.

**Why these reds are honest, and why there is no third behaviour.** Both
reds are assertion-level, taken over content that is absent at the time.
No module fails to load and no fixture is broken on purpose. The
candidate third behaviour, "the generated files equal a fresh render"
(render parity), has no honest red. At HEAD the committed files already equal the render
(measured 2026-09-24, zero diff), and the existing `instructions.stale`
check rule already guards it. That behaviour goes to Acceptance as the
empty re-run diff and a clean `instructions.*` check, not to a behaviour
with a manufactured red.

## Acceptance

Every item is a command run from `<WT>` at the builder's last commit.

- `node scripts/ci-workflow.test.mjs` exits 0. Its output contains no
  line starting with `FAIL`.
- `npm run -s bisellium -- ci` exits 0. That covers typecheck, lint,
  format:check, the full `npm test` and both officina checks.
- **Render parity:**
  `npm run -s bisellium -- instructions --studio studio --repo . --write && git status --porcelain`
  prints nothing. This proves the committed bytes equal the render. It
  cannot prove *how* they were produced: a hand edit that happens to match
  byte for byte is the same artifact. The builder's step-3 transcript
  (below) is the only provenance evidence, and the censor reads it.
- **ADOPTION is verbatim.** The assertions only catch missing needles, so
  a section that is skeletal but carries every needle passes them. This
  diff catches it:
  `diff <(sed -n '/^````$/,/^````$/p' studio/briefs/W-050.md | sed '1d;$d') <(sed -n '/^## Running ci$/,/^## Writing to a studio/p' docs/ADOPTION.md | sed '$d' | sed '$d')`
  prints nothing and exits 0.
- **No instructions or red findings:**
  `npm run -s check -- studio --repo . --json | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const f=JSON.parse(s).findings.filter(x=>/^(instructions\.|cap\.instructions$|opus\.red_)/.test(x.rule));console.log(f.length);process.exit(f.length?1:0)})'`
  prints `0` and exits 0. That means no `instructions.stale`,
  `instructions.present`, `cap.instructions`, `opus.red_evidence` or
  `opus.red_content` finding.
- **Reds:**
  - `grep -c '^FAIL' studio/ci/reds/W-050/01.log` is at least `3`, and
    `grep -F 'FAIL  CLAUDE.md carries the ci line' studio/ci/reds/W-050/01.log`
    matches.
  - `grep -F 'PASS  CLAUDE.md carries the ci line' studio/ci/reds/W-050/02.log`
    matches, and so does
    `grep -F 'FAIL  ADOPTION.md: "## Running ci" follows' studio/ci/reds/W-050/02.log`.
  - Both headers carry `# exit: 1` and `# sella: builder-sol`.
- **Diff scope:** `git diff --name-only $(git merge-base master HEAD) HEAD`
  lists exactly these paths:
  - `AGENTS.md`, `CLAUDE.md`, `docs/ADOPTION.md`
  - `packages/cli/src/instructions.template.md`,
    `scripts/ci-workflow.test.mjs`
  - `studio/ci/reds/W-050/01.log`, `studio/ci/reds/W-050/02.log`,
    `studio/opera/W-050.md`

  The orchestrator's later `verify` adds `studio/ci/W-050-*.log`.
  `git diff $(git merge-base master HEAD) HEAD -- CLAUDE.md AGENTS.md packages/cli/src/instructions.template.md`
  shows exactly one added line per file, and it is `CI_LINE`.
- **Commit order:** the commit that adds `01.log` does not touch the
  template, `CLAUDE.md`, `AGENTS.md` or `ADOPTION.md`. The template,
  `CLAUDE.md` and `AGENTS.md` change in one commit, and `ADOPTION.md` does
  not change in it. Check with
  `git log --name-only --format=%h $(git merge-base master HEAD)..HEAD`.
- **The censor's mutation run** (scratch copies, kept in the review log,
  never committed). Each mutation must make
  `node scripts/ci-workflow.test.mjs` exit 1:
  - Delete `CI_LINE` from the template only.
  - Delete `npm run -s typecheck` from the ADOPTION section. Pick this
    command because it appears there only once: the studio check appears
    twice, so deleting one occurrence would leave it matched.
  - Move the section above `## Running run and verify`.
  - Append a seventh entry to `CI_STEPS` in `ci.ts`.
  - Add a flag to `ci.ts`'s `USAGE`.

## Out of scope

- `.github/workflows/ci.yml`, `packages/commands/src/ci.ts` (`CI_STEPS`,
  `USAGE`) and `package.json`: no change. The guard stays in the existing
  script, so no new test file needs to be added to `npm test`.
- `docs/STUDIO.md` (see Intent).
- `CONTRIBUTING.md`'s ci.yml bullet, `README.md`'s build order,
  `docs/INSTRUCTIONS.md`'s slash-command table and a
  `.claude/commands/ci.md`. The CONTRIBUTING bullet predates the job
  split and does not mention `ci`. It is a candidate follow-up, and filing
  it is the orchestrator's call.
- A new check rule. `instructions.stale` already enforces render parity.

## For the trial builder (gpt-5.6-sol)

Your seat is `builder-sol`, declared in `studio/bisellium.yml`. Record
every red with `--sella builder-sol` and run the handoff with
`--sella builder-sol`, exactly as the commands below do. Never use another
sella id.

Run every command exactly as written, from `<WT>`, the worktree the
orchestrator gave you, checked out on `opus/W-050`. If any output differs
from what is stated, **stop and report it verbatim. Do not work around
it.** In particular, stop and report if a command fails for sandbox
reasons. Never:

- run `ready`, `verify`, `review`, `done`, `branch`, `merge`, `close` or
  `git push`, or pass `--no-verify`;
- hand-edit anything under `studio/`, `CLAUDE.md`, `AGENTS.md` or
  `GLOSSARY.md`;
- run `red` twice for one behaviour (it overwrites the log);
- use `git checkout --` or `git stash`.

Every commit message ends with a blank line and then
`Co-Authored-By: builder-sol (gpt-5.6-sol) <noreply@openai.com>`.

0. **Boot.**
   - `export BISELLIUM_SELLA=builder-sol`
   - `npm run -s bisellium` prints the usage banner. Read it.
   - `git rev-parse --abbrev-ref HEAD` prints `opus/W-050`.
   - `grep '^state:' studio/opera/W-050.md` prints `state: building`.
   - `git status --porcelain` prints nothing.
   - `node scripts/ci-workflow.test.mjs; echo "exit=$?"` prints
     `exit=0`.
1. **Write the tests.** Add the assertions from "The new assertions" to
   `scripts/ci-workflow.test.mjs`, and touch nothing else. Then:
   - `node_modules/.bin/prettier --write scripts/ci-workflow.test.mjs`
   - `node_modules/.bin/eslint scripts/ci-workflow.test.mjs` shows no
     output.
   - `node scripts/ci-workflow.test.mjs; echo "exit=$?"` ends in
     `exit=1`. The 14 pre-existing checks are `PASS`, and every new check
     is `FAIL`. If any pre-existing check fails, stop.
2. **Red 1:**
   `npm run -s bisellium -- red W-050 --behaviour 1 --sella builder-sol --studio studio --repo . -- node scripts/ci-workflow.test.mjs`.
   It prints `W-050: red recorded for behaviour 1 -> ci/reds/W-050/01.log`.
   Then:
   - `git add scripts/ci-workflow.test.mjs studio/ci/reds/W-050/01.log`
   - `git commit -m "test(W-050): ci docs drift assertions, red for behaviour 1" -m "Co-Authored-By: builder-sol (gpt-5.6-sol) <noreply@openai.com>"`
3. **Behaviour 1.** Insert `CI_LINE` into the template as specified. Then:
   - `npm run -s bisellium -- instructions --studio studio --repo . --write`
   - `git status --porcelain` prints exactly
     ` M AGENTS.md`, ` M CLAUDE.md` and
     ` M packages/cli/src/instructions.template.md`.
   - `git diff --numstat` shows `1	0` for each of the three.
   - `node scripts/ci-workflow.test.mjs` shows the three behaviour-1
     checks `PASS` and the behaviour-2 checks still `FAIL`.
   - `git add AGENTS.md CLAUDE.md packages/cli/src/instructions.template.md`
   - `git commit -m "docs(W-050): bisellium ci in the instructions template; CLAUDE.md and AGENTS.md regenerated" -m "Co-Authored-By: builder-sol (gpt-5.6-sol) <noreply@openai.com>"`
4. **Red 2:**
   `npm run -s bisellium -- red W-050 --behaviour 2 --sella builder-sol --studio studio --repo . -- node scripts/ci-workflow.test.mjs`.
   Then
   `diff <(sed '1,/^$/d' studio/ci/reds/W-050/01.log) <(sed '1,/^$/d' studio/ci/reds/W-050/02.log)`
   must show the three behaviour-1 lines changing from `FAIL` to `PASS`.
5. **Behaviour 2.** Insert the ADOPTION block exactly as specified. Then:
   - `node scripts/ci-workflow.test.mjs; echo "exit=$?"` prints
     `exit=0`.
   - `git add docs/ADOPTION.md studio/ci/reds/W-050/02.log`
   - `git commit -m "docs(W-050): Running ci in ADOPTION, red for behaviour 2" -m "Co-Authored-By: builder-sol (gpt-5.6-sol) <noreply@openai.com>"`
6. **Handoff.**
   - `npm run -s bisellium -- handoff --opus W-050 --sella builder-sol --stage building --next "Behaviours 1-2 green; orchestrator runs verify, then Terra pre-review and censor round 1" --studio studio`
   - `git add studio/opera/W-050.md`
   - `git commit -m "chore(W-050): traditio handoff for verify" -m "Co-Authored-By: builder-sol (gpt-5.6-sol) <noreply@openai.com>"`
7. **Prove it.** Run every Acceptance command except the mutation run,
   in the order listed. `npm run -s bisellium -- ci` takes minutes.
   `git status --porcelain` prints nothing at the end.
8. **Report.** Give:
   - every commit hash;
   - the exit status of each Acceptance command;
   - the verbatim terminal transcript of step 3, from the
     `instructions --write` command through the `git diff --numstat`
     output. This is the provenance evidence for the regenerated files.

Then the orchestrator, in `<WT>`, runs
`npm run -s bisellium -- verify W-050 --studio studio --repo .` and
commits the result. Terra's pre-review and the censor follow as usual.

## For Sol (red-team targets)

- **Stub-proofing.** Behaviour 1 matches an exact line in an exact
  position. Behaviour 2 derives its needles from `CI_STEPS` and from
  `ci.ts`'s `USAGE`, so any section missing a needle fails. A paraphrase,
  or a skeletal section that keeps every needle, still passes the
  assertions. The verbatim diff in Acceptance is what catches those. Can a
  stub get past both?
- **Known residual: one direction only.** Removing a step from `CI_STEPS`
  does fail the existing workflow comparison. But the documentation
  assertions do not reject the step's text left behind in the "Today
  those are" sentence. This is accepted. Challenge it if it matters.
- **Red distinctness rests on sequencing:** red 2 comes after
  behaviour 1 is green. Is there a path where a builder follows the steps
  and still produces identical bodies, or a red that is not
  assertion-level?
- **Prose accuracy.** Check each ADOPTION claim against `ci.ts` and
  `ci.yml` (see the mapping under Interfaces). Watch for the `--opus` exit
  code and the `--allow-dirty` wording in particular.
- **The `builder-sol` precondition.** Is a declared seat the right
  attribution, or does it conflict with D-020's "codex models write no
  officina bookkeeping except through the CLI"? Here the orchestrator
  writes the manifest line and Sol writes only through `red`/`handoff`.
