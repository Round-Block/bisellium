# W-057 — the server does not expose stack traces: GHAS alert 3 closed

Spec of record, signed by the architect, 2026-09-25. **Revision 2**, after
Sol's red-team: AMEND-FIRST, 3 lands / 2 held. Land 3 replaced the enforcement
mechanism, land 1 corrected the class, and land 5 unbundled a red. Sol has not
reviewed revision 2.

Small, and security-scoped: one helper changes, one interpolation is dropped,
and the acceptance is an external alert flipping to fixed.

## Intent

GHAS alert 3, `js/stack-trace-exposure`, medium, **open**, flagged at
`apps/server/src/http.ts:108` — the `res.end(text)` inside `sendJson`.

The subtlety worth stating first, because it is why this has stayed open: the
code **already** withholds the stack. `serverError` (`:120-124`) reads

```ts
/** Never leaks a stack — the message only (spec: "500 never leaks a stack"). */
function serverError(res: ServerResponse, e: unknown): void {
  if (res.headersSent) return;
  sendJson(res, 500, { error: e instanceof Error ? e.message : String(e) });
}
```

That comment is true about `.stack` and **false about the alert**. CodeQL's
taint model treats an exception-derived **message** as stack-derived
information, so the flow `catch (e) → e.message → sendJson → res.end` is the
reported flow and dropping `.stack` never touched it. A reviewer reading the
comment concludes the alert is a false positive; it is not.

It is also not merely a model quirk. `route()`'s catch
(`:867`) receives **any** unexpected throw from any handler, and those
messages carry real server detail — an `ENOENT` names an absolute path, a git
failure names a ref, a YAML error names a line in a file the client cannot
see. The alert is pointing at a genuine disclosure channel that happens to be
untested: **nothing in `apps/server/test/server.test.ts` asserts on a 500 body
at all** (measured).

## The class, not the instance

One line is flagged. There are **two response sinks** that can serialise an
arbitrary caught value, plus one transformation that feeds one of them.
Revision 1 called this "four response flows", which conflated a transformation
with a sink and understated what reaches `:184` — land 1. Measured across
`http.ts`, the **only** HTTP responder in the tree (`index.ts` and `store.ts`
write no responses):

| site | kind | status | carries |
|---|---|---|---|
| `:123` `serverError` | **sink** | 500 | `e.message` / `String(e)` from **any** throw in `route()` — the flagged flow |
| `:184` `readJsonBodyOr400` | **sink** | 400 | `e.message` / `String(e)` of **any** rejection that is not `PayloadTooLargeError` |
| `:163` | transformation | — | wraps `JSON.parse`'s message into a new `Error`; reaches `:184` |
| `:181` | guarded | 413 | `e.message` of `PayloadTooLargeError`, narrowed by `instanceof` — a constant **by the narrowing**, not by luck |
| `:117` `notFound` | — | 404 | a caller-supplied route noun, never a caught value |
| `:499` | — | 503 | a string literal — **the model this opus copies** |
| `:830`, `:865` | — | — | `e.message` to **stderr**, the correct destination |
| `:764` | — | — | calls `readJsonBody` and **discards** the error (`.catch(() => ({}))`) — correctly not a sink |

**`:184` is the finding revision 1 missed.** Its `catch` does not only receive
the two `Error`s `readJsonBody` constructs. The last line of that function is

```ts
    req.on("error", reject);
```

— `:166`, which rejects with the **raw request-stream error, unwrapped**. So a
socket-level failure (`aborted`, `ECONNRESET`, a TLS or proxy error under a
future W-070 deployment) has its message serialised into a 400 body. Whether it
is delivered depends on socket state, which is not a guarantee either way; the
flow exists in code and that is what the taint model reports and what an
attacker would probe.

`:499` already does the right thing: `sendJson(res, 503, { error: "too many
/api/live clients" })`. This opus makes 500 look like that.

## Design

**1. The 500 body becomes a constant plus a correlation id.**

```ts
{ error: "internal error", ref: "<uuid>" }
```

`randomUUID` comes from `node:crypto`, which `http.ts` **already imports**
(`:33`, for `randomBytes`/`timingSafeEqual`) — an extended import, not a new
dependency. Full uuid rather than a truncation: an 8-character id reads nicer
in a log and invites a collision argument nobody needs to have.

**The `ref` is the only correlator available, verified** (Sol's held 2). The
server's two log writers carry no wall-clock time: `:830` writes
`bisellium serve: poll failed: <msg>` and `:865` writes
`<method> <url> <status> <ms>` — a duration, not a timestamp. So an operator
handed a 500 cannot correlate it by time even with full stderr access, and
revision 1's own For Sol target questioning whether `ref` earns its place is
answered: it does, and the reason is a gap in the logs rather than a
preference.

**2. The real error goes to the server log, with the same `ref`.** Message
**and stack**, to `process.stderr`, matching `:830`'s existing precedent for
server diagnostics:

```
bisellium serve: 500 <ref> <message>
<stack>
```

The stack is *why* the log exists. Withholding it from the client and
discarding it entirely would trade a security finding for an operability
one — an operator with the `ref` from a console screenshot must be able to
find the throw.

**3. The interpolated parse message is dropped** (`:163`):
`` `invalid JSON body: ${(e as Error).message}` `` becomes the constant
`"invalid JSON body"`. Not because a V8 parse message is sensitive — it
describes the client's own input — but because it is the only remaining path
by which an arbitrary exception's text reaches a response body. Dropping it
makes the invariant below **total over the file** rather than true of the 500
alone, and a total invariant is testable by inspection.

The cost, named: a client sending malformed JSON loses "Unexpected token } at
position 42". The only client is this repo's own console, and the status plus
the request is enough to debug it.

**4. The invariant is enforced at the response boundary by the compiler, not
by a grep** (land 3). Revision 1 proposed a source pin asserting that no
`catch` binding reaches a `sendJson` argument. Sol is right that this is not an
invariant: assigning `const msg = e.message` two lines earlier defeats it, and
a brand whose constructor takes a `string` just relocates the hatch.

So the error body gets **one choke point with a closed input set**:

```ts
/** Every `error` value any response may carry, as a closed union of literals.
 *  A caught value cannot be a member of this type, so `sendError` cannot be
 *  handed one — enforced by tsc. */
type ErrorCode =
  | "internal error"
  | "invalid JSON body"
  | "request body must be a JSON object"
  | "request body too large"
  | "too many /api/live clients";

function sendError(res: ServerResponse, status: number, code: ErrorCode, ref?: string): void {
  sendJson(res, status, ref === undefined ? { error: code } : { error: code, ref });
}
```

`sendError(res, 500, e.message)` is now a **typecheck failure**, and so is
every aliased, interpolated or laundered variant of it, because the type admits
five strings and nothing else. That is the difference between an invariant and
a convention: `npm run -s typecheck` is the enforcement, and behaviour 3's pin
only has to assert that the choke point still exists.

**Because the input set is closed, `:184` must classify rather than
forward** — which is how land 1's arbitrary-value path closes. `readJsonBody`
rejects with **typed classes** instead of message-bearing `Error`s
(`PayloadTooLargeError` already exists; add `MalformedJsonError` and
`NotAnObjectError`), and `readJsonBodyOr400` dispatches on `instanceof`,
mapping each to its literal code. **Anything unrecognised is rethrown**, so a
request-stream error from `:166` lands in the 500 path with a `ref` and a log
entry instead of being narrated to the client.

That is a deliberate behaviour change and it is more honest than the status
quo: a socket failure is not a diagnosed client mistake, so answering 400 with
`aborted` claims a diagnosis we did not make. Behaviour 6 pins it.

`notFound` (`:117`) stays outside `sendError`: its input is a route noun the
server itself chose, never a caught value. That is the one documented exception
to "all error bodies go through the choke point", and behaviour 3's pin names
it explicitly rather than leaving a reader to wonder.

**5. What does not change.** Statuses, shapes, and the 4xx/503 contract. 400
still answers a malformed body, 413 an oversized one, 404 an unknown thing,
503 a full SSE pool; every one still returns `{ error: <string> }`. The two
existing tests that touch these assert status and `typeof body.error ===
"string"` (`server.test.ts:438-439`, `:411`) and both keep passing unchanged —
verified by reading them, which is why this opus needs no fixture edits.

## Files owned

- `apps/server/src/http.ts` — `ErrorCode`, `sendError`, `MalformedJsonError`
  and `NotAnObjectError`, `serverError`, `readJsonBody`'s three typed
  rejections, `readJsonBodyOr400`'s `instanceof` dispatch and rethrow, the
  `randomUUID` import, and the comment at `:120` that currently misleads.
- `apps/server/test/server.test.ts` — the behaviours. It has no stderr helper;
  add a local one that swaps `process.stderr.write` and restores it in a
  `finally` (the shape `withStderr` uses elsewhere in the tree).
- `docs/ADOPTION.md` — only if it documents the 500 body shape. **Check
  before editing**; if it says "500 returns the error message", it is now
  wrong and must change.

Not owned: `apps/server/src/index.ts`, `store.ts` (no responses),
`.github/workflows/ci.yml` (CodeQL runs through GitHub's **default setup** —
there is no in-repo CodeQL config to change, verified).

## Behaviours to test

1. **A 500 body carries nothing from the exception.** Inject a `checkStudio`
   that throws `new Error("ENOENT: open '/home/secret/studio/bisellium.yml'")`
   — the existing `StartServerOptions` seam, used by `GET /api/health`
   (`:631`), which reaches `route()`'s catch at `:867`. Then: status 500;
   `body.error === "internal error"` exactly; `body.ref` is a non-empty
   string; and the serialised body contains **neither** `ENOENT` **nor**
   `/home/secret` **nor** the word `Error`. Assert on the raw response text,
   not the parsed object, so a nested field cannot smuggle it.

2. **The exception reaches the log, correlated by `ref`.** The same request
   writes to `process.stderr` a line containing the **same `ref`** as the body,
   the original message, and a stack. Assert the `ref` from the body appears in
   captured stderr — correlation is the whole point, and per the design note it
   is the *only* correlator, since neither `:830` nor `:865` logs a timestamp.

3. **The choke point exists and is closed.** Two halves, and the first is the
   real enforcement:
   - **`npm run -s typecheck` fails** on a synthetic edit that passes an
     exception-derived value to `sendError` — `sendError(res, 500, e.message)`
     and the aliased variant `const msg = e.message; sendError(res, 500, msg)`.
     Both must be compile errors, because `ErrorCode` admits five literals and
     nothing else. This is the positive control that revision 1's grep could
     not have: aliasing defeats a grep and cannot defeat a union type.
   - **A source pin** asserts the choke point has not been bypassed: no
     `sendJson` call in `http.ts` carries a 4xx or 5xx status, with exactly two
     documented exceptions — `sendError` itself and `notFound` (`:117`), whose
     input is a route noun. The pin's job is narrow: not "no tainted value
     reaches a response", which the type now guarantees, but "nobody routed
     around the type".

4. **Malformed JSON answers the constant.** A POST with `{` as its body gets
   status 400 and `error === "invalid JSON body"` **exactly** — no position
   text, no V8 wording. This has its own honest red (land 5): today the same
   request yields `invalid JSON body: Unexpected token ...`, so an exact-equality
   assertion fails before the fix. Revision 1 bundled this red with behaviour
   1's on the grounds that it "passes today", which was simply wrong — and
   bundling would have violated the one-red-per-behaviour contract this brief
   inherits (`W-072.md`'s reds section).

5. **Two 500s get two different `ref`s.** Two requests, two ids, both present
   in the log. Pins that `ref` is generated per response rather than a constant
   placeholder that would correlate nothing.

6. **A request-stream error is not narrated to the client** (land 1). Induce a
   rejection on the `:166` path — destroy or error the request stream mid-body
   — and assert the response is **not** a 400 carrying the stream error's
   message. It reaches the 500 path: `error === "internal error"`, a `ref`, and
   the stream error's own message in the log. If the socket is no longer
   writable the assertion is on the **log** alone, which is the honest
   outcome and must be stated in the test rather than skipped: the property is
   "the message never becomes a response body", and an undeliverable response
   satisfies it.

7. **The remaining contract is unchanged.** A non-object JSON body → 400
   `"request body must be a JSON object"`; an oversized body → 413
   `"request body too large"`; an unknown route → 404 as before; a full SSE
   pool → 503 as before. The two pre-existing assertions at
   `server.test.ts:411` and `:438-439` must still pass **untouched** — if the
   diff edits either, the change was wider than specified.

Reds via `bisellium red W-057 --behaviour <n> --sella <id> --studio studio --
<cmd>`, one per behaviour, assertion-level, **no two sharing an output**
(engineering lex §2). Every behaviour above reds on its own against today's
tree: 1, 2, 5 and 6 because `serverError` sends the message and writes no log;
3 because neither `ErrorCode` nor the pin exists; 4 because the message carries
the V8 position text. Behaviour 7 is the exception — it passes today by
construction, being a regression guard for what must **not** change. Give it a
mutation red (delete one of the three literal codes and watch it fail) rather
than bundling it, for the reason land 5 makes plain.

## Acceptance

1. `npm test` green; `typecheck`, `lint`, `format:check` clean. **`typecheck`
   is not a formality here** — it is the mechanism enforcing the invariant
   (behaviour 3), so a green typecheck with the `ErrorCode` union in place is
   itself an acceptance artefact.
2. **GHAS alert 3's `fixed_at` flips.** CodeQL runs on the PR through the
   default setup; the acceptance is the alert moving to fixed, not the code
   changing. Record the alert number and the `fixed_at` timestamp in the
   opus's evidence.
3. **No new `js/stack-trace-exposure` alert takes its place.** Fixing one
   taint flow can shift the report to a sibling, which is exactly why this opus
   closes **both sinks** — `:123` and `:184` — rather than the flagged line.
   Check the full alert list after the scan, not just alert 3.
4. **No 500 body in any test output contains a filesystem path.** Grep the
   suite's captured output for `/home/` — a cheap, total check on the property
   the alert is really about.
5. The merge gate's alert-3 exclusion is removed (Sequence).

## Sequence

- **W-070 (tailnet exposure) explicitly waits on this.** That ordering is the
  reason this opus is worth its size now rather than later: W-070 publishes
  the console beyond localhost, and a stack-trace channel is worth far more to
  an attacker who can reach it. The ranking acta
  (`acta/2026-09-25-ranking.md`) already puts W-057 ahead of W-070 for this
  reason; landing them the other way round would be the expensive order.
- **The merge gate's alert-3 exclusion becomes obsolete.** The orchestrator's
  gate scripts carry an exclusion so that alert 3 does not block a merge.
  Once `fixed_at` is set, that exclusion is dead code that would silently
  swallow a *future* stack-trace alert. It lives in the orchestrator's own
  scripts, not in this repo — I grepped `.github/` and `scripts/` and there is
  no alert allowlist committed here — so **removing it is the coordinator's
  action, named here so it is not forgotten.**

## House constraints

TypeScript strict + `noUncheckedIndexedAccess`; ESM `.js` import specifiers;
Node ≥ 25; `packages/cli/src/check.ts`'s house style. No new dependency —
`randomUUID` extends the existing `node:crypto` import.

## Out of scope

- **Structured logging.** One stderr line matching `:830`'s existing shape.
  A logger is a separate decision.
- **Returning `ref` on 4xx.** Client mistakes need no correlation id; the
  client has the request.
- **Rate-limiting or auditing 500s.** Different opus, different argument.
- **The other GHAS alerts.** This opus closes alert 3 and asserts no
  `js/stack-trace-exposure` replaces it. Any other rule's alerts are theirs.
- **Changing what CodeQL scans.** No in-repo config exists and none is added;
  suppressing an alert by configuration would be the opposite of this opus.

## For Sol

Sol keeps the slot; not a UI opus.

**Revision 1's five targets are resolved.** Target 1 (the alert set) is
unchanged and still live, below. Target 2 (does `ref` earn its place) is
**answered — it does**, because neither `:830` nor `:865` logs a timestamp, so
time is not an available correlator. Target 3 (the grep pin) became land 3 and
the pin is replaced by a closed union the compiler checks. Target 4 (dropping
the parse position) is **settled**: genericise at the boundary, all statuses,
not the 500 alone. Target 5 (behaviour 4's bundled red) became land 5 and was
simply my error — that red is distinct and always was.

1. **I still cannot verify the alert set, and acceptance 3 depends on it.**
   Carried from revision 1 unchanged because nothing has changed: the flows are
   measured in source, but "only `:123` is flagged" is the coordinator's report
   and I have no GHAS access. Land 1 makes this sharper rather than softer —
   `:184` is now known to sink an arbitrary caught value, so if CodeQL is not
   already flagging it, either its model stops at the `new Error` wrapper or the
   alert list is longer than reported. **Check the list.** My least-confident
   claim, and the only one I cannot close by measurement.
2. **Rethrowing an unrecognised body error changes an observable status**, from
   400 to 500, on the `:166` path. I argue that is more honest — a socket
   failure is not a diagnosed client mistake — but it is a contract change
   smuggled into a security fix, and a client that today branches on 400 for
   "bad request" would see a 500. Nothing in this repo's console does, as far
   as I can see. Attack that: find the consumer, or tell me the honest move is
   a 400 with a generic `"invalid request body"` code and no status change.
3. **`ErrorCode` as a closed union has a maintenance edge.** Every new error
   response must add a member, and the lazy way past a type error is to widen
   the union with something vague, or to add `| string`, which silently
   restores the hatch. The type cannot defend itself against that. Is the pin
   in behaviour 3 the right guard for it, or should the union's *shape* be
   asserted — e.g. a test that fails if `ErrorCode` admits a non-literal?
4. **The 413 path's safety is by `instanceof`, not by the union.** After this
   opus `:181` maps `PayloadTooLargeError` to a literal, which is correct — but
   the reason it was *already* safe is a narrowing three lines up, and
   narrowings get refactored. Does the dispatch need a default-case
   exhaustiveness check so a fourth rejection class cannot silently fall into
   the wrong code?

## Amendment log

| Revision | Date | Change | Authority |
|---|---|---|---|
| 1 | 2026-09-25 | Spec of record, 5 behaviours | architect |
| 2 | 2026-09-25 | Sol red-team, AMEND-FIRST (3 lands / 2 held). Land 3 replaced the enforcement mechanism; land 1 corrected the class and added a behaviour; land 5 unbundled a red. 7 behaviours. | architect, on Sol's findings |

Revision 2, land by land, each measured before adoption:

1. **Land 1 — the class was misstated, and the gap was real.** `:163` is a
   transformation, not a response flow; calling it one inflated "four flows"
   out of two sinks. More importantly `:184` does not merely carry our two
   constructed messages: `readJsonBody`'s last line is `req.on("error",
   reject)` (`:166`), which rejects with the **raw request-stream error**, so a
   socket failure's message was serialisable into a 400 body. Closed by having
   `readJsonBody` reject with typed classes and `readJsonBodyOr400` **rethrow**
   anything unrecognised into the 500 path. New behaviour 6 pins it. Also
   measured while checking: `:764` calls `readJsonBody` directly but discards
   the error, so it is correctly not a sink — no fourth one exists.
2. **Land 3 — the grep pin is replaced by a type.** Sol is right that it was
   not an invariant: `const msg = e.message` defeats it, its wording named an
   `error` field on `sendText` that does not exist, and a brand with a
   `string` constructor relocates the hatch rather than closing it. The error
   body now has one choke point, `sendError(res, status, code: ErrorCode)`,
   whose `ErrorCode` is a closed union of five literals — so passing a caught
   value is a **typecheck failure**, aliased or not. The pin shrinks to its
   honest job: asserting nobody routed around the choke point. `notFound` is
   the one named exception, since its input is a route noun.
3. **Land 5 — behaviour 4's red is distinct and always was.** Today malformed
   JSON answers `invalid JSON body: <V8 message>`, so asserting
   `error === "invalid JSON body"` exactly fails before the fix. I had claimed
   it "passes today" and bundled its red, which was wrong on the facts and
   would have breached the one-red-per-behaviour contract this brief inherits.
   Unbundled. Behaviour 7 is now the only regression guard, and it gets a
   mutation red rather than a shared one.
4. **Held 2 — `ref` earns its place, verified.** Neither of the server's log
   writers carries a wall-clock timestamp (`:830` writes a message, `:865` a
   duration), so an operator cannot correlate a 500 by time even with full
   stderr. The design note now says so, and revision 1's For Sol target on it
   is struck.
5. **Held 4 — genericise at the boundary, not only the 500.** Adopted as the
   whole shape of land 3's fix: every error body, every status, goes through
   one closed choke point, and the parse position is dropped.
6. **Found while sweeping rev 2:** acceptance 3 still read "closes all four
   flows", the framing land 1 had just corrected two hundred lines above. That
   is the spec-revision-residue class in my own text again — the fifth
   instance, and the reason W-084 exists. Corrected to "both sinks". The sweep
   that caught it is now the last step of every revision I write.
