# W-023 — containment scoping and helper hoist (P-004)

## Intent

P-004: `path.escapes.officina` fired on legitimate acta evidence
(`../README.md` from inside `studio/`). The Patron decreed option (b):
scope the rule to contractually officina-relative surfaces (opus `spec:`
and gate `evidence:` paths only), remove acta and lessons evidence checks,
and re-promote both path rules to `block`.

## Files owned

- `packages/cli/src/rules/paths.ts` — scope narrowed, level promoted
- `packages/cli/src/rules.test.ts` — test comment updated

## Behaviours to test

1. `path.id.unvalidated` fires at `block` level
2. `path.escapes.officina` fires at `block` for spec and gate evidence
3. Acta evidence escaping does NOT fire (scoped out)
4. Real studio has zero blocking path.* findings

## Acceptance

Both path rules promoted to blocking. The insurance clause test passes
on the real `studio/` tree.

## Out of scope

- Retro numbering defect (P-006, separate)
