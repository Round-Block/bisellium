node_modules/
dist/
.bisellium/
package-lock.json
*.md
studio/
examples/
docs/
# .claude/** and cascades/** belong to other opera this cascade (W-017,
# W-018 — both halted) — reformatting them is not this opus's to do.
.claude/
cascades/
# cascade 5 (W-016/W-019): packages/, apps/ and adapters/ are mid-migration
# this cascade (files move between builders) — widening this ignore back
# down to individual files, then dropping it, is the first task of the next
# cascade once the moved files settle (see CONTRIBUTING.md).
packages/
apps/
adapters/
# W-018's script (cascades/ package, not this opus's file) — new since that
# brief was written, not owned here. The `scripts/opus-ready.ts` entry that
# sat beside it went out with the file itself (W-020, cascade 6).
scripts/usage-from-workflow.mjs
# Claude Code sandbox /dev/null masks (see .gitignore) — unreadable device
# nodes prettier's "." glob would otherwise choke on.
.bash_profile
.bashrc
.profile
.zprofile
.zshrc
.gitconfig
.gitmodules
.ripgreprc
.mcp.json
.idea
.vscode
